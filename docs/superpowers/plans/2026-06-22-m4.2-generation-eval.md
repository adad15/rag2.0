# M4.2 生成侧评估（条款引用 + 数值准确率）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 `rag2 eval` 加 opt-in `--gen` 开关：对带生成 gold 的用例调 `answer_query` 拿答案文本，自动打两个纯测量指标——条款引用准确率、数值准确率，带答案磁盘缓存保证可复现。

**Architecture:** 非侵入（不改 `answer_query` 签名）。新增纯指标模块 `generation_metrics`（可全量 TDD）+ 编排模块 `generation_eval`（infra-bound，靠实跑验证）。`generation_eval` 通过磁盘缓存包装复用现成 `answer_query`，缓存键 = `hash(normalize_question + prompt_version)`（复用 `query_planner.h` 的 `normalize_question`）。`cmd_eval` 加 `bool gen`，`--gen` 时在检索报告后追加生成报告。

**Tech Stack:** C++20、MSBuild + vcpkg、nlohmann/json、spdlog、doctest、`<filesystem>`。沿用 [[rag2-build-setup]]。

**Scope:** 见 spec [`2026-06-22-m4.2-generation-eval-design.md`](../specs/2026-06-22-m4.2-generation-eval-design.md)。只做引用+数值两个纯测量指标；不做拒答/幻觉/阈值标定/LLM-judge；不改 `answer_query` 签名、不改检索逻辑；不扩到~100。分支 V3.3。

**构建/测试（PowerShell，项目根目录）：**
```powershell
& "D:\vs2022\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" rag2.0.sln /p:Configuration=Debug /p:Platform=x64 /m
.\rag2.0.tests\x64\Debug\rag2.0.tests.exe
```
当前基线：174 用例全绿。

---

## Task 1: `EvalCase.gold_values` + `parse_dataset`

**Files:**
- Modify: `src/eval/dataset.h`, `src/eval/dataset.cpp`
- Test: `tests/test_dataset.cpp`

- [ ] **Step 1: 在 `src/eval/dataset.h` 给 `EvalCase` 加字段**

在 `std::vector<std::string> gold_methods;` 后加：
```cpp
    std::vector<std::string> gold_values;   // 数值题期望出现的限值/单位（空=非数值题）
```

- [ ] **Step 2: 写失败测试到 `tests/test_dataset.cpp`**

文件顶部已 `#include "eval/dataset.h"`。追加：
```cpp
TEST_CASE("parse_dataset reads gold_values array") {
    auto cases = parse_dataset(R"([
        {"question":"针入度限值","gold_values":["100","0.1mm"]},
        {"question":"无数值题","gold_method_no":"T0316-2024"}
    ])");
    REQUIRE(cases.size() == 2);
    REQUIRE(cases[0].gold_values.size() == 2);
    CHECK(cases[0].gold_values[0] == "100");
    CHECK(cases[0].gold_values[1] == "0.1mm");
    CHECK(cases[1].gold_values.empty());     // 缺字段取空
}
```

- [ ] **Step 3: 运行测试，确认失败**

```powershell
& "D:\vs2022\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" rag2.0.sln /p:Configuration=Debug /p:Platform=x64 /m
.\rag2.0.tests\x64\Debug\rag2.0.tests.exe --test-case="*gold_values*"
```
Expected：FAIL——`cases[0].gold_values.size()` 为 0（`parse_dataset` 还没读该字段）。

- [ ] **Step 4: 在 `src/eval/dataset.cpp` 实现解析**

在 `parse_dataset` 里 `gold_methods` 的解析块后（`out.push_back(std::move(c));` 之前）加：
```cpp
        if (item.contains("gold_values") && item["gold_values"].is_array())
            for (const auto& v : item["gold_values"])
                if (v.is_string()) c.gold_values.push_back(v.get<std::string>());
```

- [ ] **Step 5: 运行测试，确认通过 + 全量**

```powershell
& "D:\vs2022\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" rag2.0.sln /p:Configuration=Debug /p:Platform=x64 /m
.\rag2.0.tests\x64\Debug\rag2.0.tests.exe --test-case="*gold_values*"
.\rag2.0.tests\x64\Debug\rag2.0.tests.exe
```
Expected：`*gold_values*` PASS；全量绿（175 用例，0 failed）。

- [ ] **Step 6: Commit**

```powershell
git add src/eval/dataset.h src/eval/dataset.cpp tests/test_dataset.cpp
git commit -m @'
feat(eval): EvalCase.gold_values + parse (数值准确率 gold)

parse_dataset 读 gold_values 字符串数组，缺字段取空。引用相关 gold 字段已存在、复用。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
'@
```

---

## Task 2: 纯指标 `generation_metrics`（全量 TDD）

**Files:**
- Create: `src/eval/generation_metrics.h`, `src/eval/generation_metrics.cpp`
- Test: `tests/test_generation_metrics.cpp`
- Modify: `rag2.0/rag2.0.vcxproj`(+`.filters`), `rag2.0.tests/rag2.0.tests.vcxproj`(+`.filters`)

- [ ] **Step 1: 写 `src/eval/generation_metrics.h`**

```cpp
#pragma once
#include <string>
#include <vector>

// 归一化用于匹配：去所有空白、全角数字(０-９)→半角、ASCII 大写→小写。纯函数。
std::string normalize_for_match(const std::string& s);

// 引用命中：归一化后答案同时包含 标准号 与 条款号/方法号 → true。
// gold_standard_code 为空串时视为“无标准号要求”，只校验 gold_ref。
// gold_ref 为空串时直接返回 false（无可校验的引用）。
bool citation_hit(const std::string& answer,
                  const std::string& gold_standard_code,
                  const std::string& gold_ref);

// 数值命中数：归一化后 gold_values 里有几个作为子串出现在答案中。
int count_value_hits(const std::string& answer,
                     const std::vector<std::string>& gold_values);
```

- [ ] **Step 2: 写测试 `tests/test_generation_metrics.cpp`**

```cpp
#include <doctest/doctest.h>
#include "eval/generation_metrics.h"

TEST_CASE("normalize_for_match strips whitespace, folds fullwidth digits, lowercases ascii") {
    CHECK(normalize_for_match("JTG 3420") == "jtg3420");
    CHECK(normalize_for_match("１００") == "100");        // 全角数字
    CHECK(normalize_for_match(" T0521 \n") == "t0521");
}

TEST_CASE("citation_hit needs both standard code and ref present") {
    std::string ans = "依据《公路工程水泥及水泥混凝土试验规程》（JTG 3420-2020）第 5.1.2 条……";
    CHECK(citation_hit(ans, "JTG 3420-2020", "5.1.2"));     // 双含
    CHECK_FALSE(citation_hit(ans, "JTG 3420-2020", "9.9.9")); // 缺条款号
    CHECK_FALSE(citation_hit(ans, "JTG 9999", "5.1.2"));      // 缺标准号
}

TEST_CASE("citation_hit with empty standard code checks only the ref") {
    std::string ans = "T0521 水泥混凝土拌合物试验需要……";
    CHECK(citation_hit(ans, "", "T0521"));                   // 无标准号要求，只验方法号
    CHECK_FALSE(citation_hit(ans, "", "T9999"));
}

TEST_CASE("citation_hit empty ref is never a hit") {
    CHECK_FALSE(citation_hit("任意答案", "JTG 3420", ""));
}

TEST_CASE("count_value_hits counts normalized substring matches") {
    std::string ans = "压力机示值精度不低于 1%，最大荷载 1000 kN。";
    CHECK(count_value_hits(ans, {"1%", "1000"}) == 2);       // 全中
    CHECK(count_value_hits(ans, {"1%", "2000"}) == 1);       // 部分中
    CHECK(count_value_hits(ans, {"１０００"}) == 1);          // 全角等价（归一化后 1000）
    CHECK(count_value_hits(ans, {}) == 0);                   // 空 gold
}
```

- [ ] **Step 3: 写 `src/eval/generation_metrics.cpp`**

```cpp
#include "eval/generation_metrics.h"
#include <cctype>

std::string normalize_for_match(const std::string& s) {
    std::string out;
    size_t i = 0;
    while (i < s.size()) {
        unsigned char c = static_cast<unsigned char>(s[i]);
        // 跳过 ASCII 空白
        if (c == ' ' || c == '\t' || c == '\r' || c == '\n') { ++i; continue; }
        // 全角数字 U+FF10..U+FF19，UTF-8 = EF BC 90 .. EF BC 99
        if (c == 0xEF && i + 2 < s.size() &&
            static_cast<unsigned char>(s[i+1]) == 0xBC) {
            unsigned char c3 = static_cast<unsigned char>(s[i+2]);
            if (c3 >= 0x90 && c3 <= 0x99) { out += static_cast<char>('0' + (c3 - 0x90)); i += 3; continue; }
        }
        if (c < 0x80) out += static_cast<char>(std::tolower(c));
        else out += static_cast<char>(c);
        ++i;
    }
    return out;
}

bool citation_hit(const std::string& answer,
                  const std::string& gold_standard_code,
                  const std::string& gold_ref) {
    if (gold_ref.empty()) return false;
    std::string a = normalize_for_match(answer);
    std::string ref = normalize_for_match(gold_ref);
    if (a.find(ref) == std::string::npos) return false;
    if (!gold_standard_code.empty()) {
        std::string sc = normalize_for_match(gold_standard_code);
        if (a.find(sc) == std::string::npos) return false;
    }
    return true;
}

int count_value_hits(const std::string& answer,
                     const std::vector<std::string>& gold_values) {
    std::string a = normalize_for_match(answer);
    int n = 0;
    for (const auto& v : gold_values) {
        std::string nv = normalize_for_match(v);
        if (!nv.empty() && a.find(nv) != std::string::npos) ++n;
    }
    return n;
}
```

- [ ] **Step 4: 登记新文件**

`rag2.0/rag2.0.vcxproj`：找到 `<ClCompile Include="..\src\eval\eval_runner.cpp" />`，在其后加：
```xml
    <ClCompile Include="..\src\eval\generation_metrics.cpp" />
    <ClCompile Include="..\src\eval\generation_eval.cpp" />
```
找到 `<ClInclude Include="..\src\eval\eval_runner.h" />`，在其后加：
```xml
    <ClInclude Include="..\src\eval\generation_metrics.h" />
    <ClInclude Include="..\src\eval\generation_eval.h" />
```
（`generation_eval.*` 本 Task 还没建文件，但先登记好；Task 3 再写文件内容。若担心空引用，可在 Task 3 再加这两行 `generation_eval`，本 Task 只加 `generation_metrics` 两行——二选一，保持登记与文件存在一致即可。）

`rag2.0/rag2.0.vcxproj.filters`：仿照 `eval_runner.cpp`/`.h` 的 `<Filter>` 条目加：
```xml
    <ClCompile Include="..\src\eval\generation_metrics.cpp"><Filter>源文件\eval</Filter></ClCompile>
    <ClInclude Include="..\src\eval\generation_metrics.h"><Filter>头文件\eval</Filter></ClInclude>
```
（Filter 名以该文件现有 `eval_runner` 条目里的实际值为准——用 `Select-String eval_runner rag2.0\rag2.0.vcxproj.filters` 确认。）

`rag2.0.tests/rag2.0.tests.vcxproj`：找到测试工程里 `eval_runner.cpp` 或 `dataset.cpp` 的被测条目，旁边加：
```xml
    <ClCompile Include="..\src\eval\generation_metrics.cpp" />
    <ClCompile Include="..\tests\test_generation_metrics.cpp" />
```
`rag2.0.tests/rag2.0.tests.vcxproj.filters`：加：
```xml
    <ClCompile Include="..\src\eval\generation_metrics.cpp"><Filter>被测源码</Filter></ClCompile>
    <ClCompile Include="..\tests\test_generation_metrics.cpp"><Filter>测试</Filter></ClCompile>
```
（Filter 名以 `Select-String test_dataset rag2.0.tests\rag2.0.tests.vcxproj.filters` 看到的实际值为准。）

- [ ] **Step 5: 运行测试，确认先 RED 后 GREEN**

```powershell
& "D:\vs2022\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" rag2.0.sln /p:Configuration=Debug /p:Platform=x64 /m
.\rag2.0.tests\x64\Debug\rag2.0.tests.exe --test-case="*citation_hit*","*normalize_for_match*","*count_value_hits*"
.\rag2.0.tests\x64\Debug\rag2.0.tests.exe
```
Expected：先确认新建测试若注释掉实现会失败（已实现则直接 PASS）；`generation_metrics` 系列全 PASS；全量绿（180 用例，0 failed）。

> 若想严格走 RED：先只加 Step 1 头 + Step 2 测试 + 空实现（函数体 `return {};`/`return false;`/`return 0;`），跑出 FAIL，再填 Step 3 真实现。

- [ ] **Step 6: Commit**

```powershell
git add src/eval/generation_metrics.h src/eval/generation_metrics.cpp tests/test_generation_metrics.cpp rag2.0/rag2.0.vcxproj rag2.0/rag2.0.vcxproj.filters rag2.0.tests/rag2.0.tests.vcxproj rag2.0.tests/rag2.0.tests.vcxproj.filters
git commit -m @'
feat(eval): generation_metrics 纯指标 (citation_hit / count_value_hits / normalize)

引用双含判定 + 数值逐值命中 + 归一化(去空白/全角数字→半角/ASCII 小写)，纯函数全量单测。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
'@
```

---

## Task 3: 编排 `generation_eval` + `kAnswerPromptVersion`（infra-bound，build-only）

**Files:**
- Modify: `src/generate/prompt_builder.h`, `src/generate/prompt_builder.cpp`
- Create: `src/eval/generation_eval.h`, `src/eval/generation_eval.cpp`
- Modify: `rag2.0/rag2.0.vcxproj`(+`.filters`)（若 Task 2 Step 4 未把 `generation_eval.*` 登记，此处补上）

- [ ] **Step 1: 给 `src/generate/prompt_builder.h` 加 prompt 版本常量**

在 `build_system_prompt` 声明前加：
```cpp
// 答案缓存键的一部分；改 build_system_prompt 必须 bump 它使旧缓存失效。
extern const char* kAnswerPromptVersion;
```
`src/generate/prompt_builder.cpp` 顶部（`build_system_prompt` 定义前）加：
```cpp
const char* kAnswerPromptVersion = "ans-v1";
```

- [ ] **Step 2: 写 `src/eval/generation_eval.h`**

```cpp
#pragma once
#include <string>
#include <vector>
#include "eval/dataset.h"
#include "milvus/milvus_rest.h"
#include "embedding/embedding_client.h"
#include "db/pg_client.h"
#include "query/synonyms.h"
#include "query/query_planner.h"
#include "generate/deepseek_client.h"

struct GenCaseResult {
    std::string question;
    bool cite_scored = false;   // 本条是否打了引用分
    bool cite_hit = false;
    int  value_gold = 0;        // 本条 gold_values 数（0=非数值题）
    int  value_hits = 0;
};

struct GenerationReport {
    int cite_scored = 0,  cite_hits = 0;
    int value_gold_total = 0, value_hit_total = 0;
    std::vector<GenCaseResult> results;
};

// 对带生成 gold 的用例调 answer_query（带磁盘缓存），打引用/数值指标。
// infra-bound：需 Milvus/PG/embedding/DeepSeek 在线。不单测，靠实跑验证。
GenerationReport run_generation_eval(
    const std::vector<EvalCase>& cases,
    milvus::MilvusRest& mv, EmbeddingClient& embed, PgClient& pg,
    const SynonymDict& syn, deepseek::DeepSeekClient& ds,
    const std::string& collection, int top_k,
    const QueryPlanner& planner, const std::string& answer_cache_dir);
```

- [ ] **Step 3: 写 `src/eval/generation_eval.cpp`**

```cpp
#include "eval/generation_eval.h"
#include "eval/generation_metrics.h"
#include "generate/answer_pipeline.h"
#include "generate/prompt_builder.h"   // kAnswerPromptVersion
#include "query/query_planner.h"       // normalize_question
#include <spdlog/spdlog.h>
#include <filesystem>
#include <fstream>
#include <sstream>
#include <functional>
#include <cstdio>

namespace {
std::string cache_path_for(const std::string& dir, const std::string& q) {
    std::string key = normalize_question(q) + "\x1f" + kAnswerPromptVersion;
    size_t h = std::hash<std::string>{}(key);
    char name[32];
    std::snprintf(name, sizeof(name), "%016zx.txt", h);
    return dir + "/" + name;
}
std::string read_file(const std::string& path) {
    std::ifstream f(path, std::ios::binary);
    if (!f) return "";
    std::stringstream ss; ss << f.rdbuf();
    return ss.str();
}
void write_file(const std::string& dir, const std::string& path, const std::string& content) {
    std::error_code ec;
    std::filesystem::create_directories(dir, ec);
    std::ofstream f(path, std::ios::binary);
    if (f) f << content;   // 写失败非致命：下次重算
}
// "T0521-2005" -> "T0521"；无 '-' 原样返回。答案多半只写不带年份的方法号。
std::string method_stem(const std::string& m) { return m.substr(0, m.find('-')); }
}  // namespace

GenerationReport run_generation_eval(
    const std::vector<EvalCase>& cases,
    milvus::MilvusRest& mv, EmbeddingClient& embed, PgClient& pg,
    const SynonymDict& syn, deepseek::DeepSeekClient& ds,
    const std::string& collection, int top_k,
    const QueryPlanner& planner, const std::string& answer_cache_dir) {

    GenerationReport rep;
    for (const auto& c : cases) {
        // 选引用 gold：方法号优先（取 stem）；否则条款号+标准号。
        std::string gold_ref, gold_std;
        if (!c.gold_method_no.empty()) {
            gold_ref = method_stem(c.gold_method_no);     // gold_std 留空：方法题无 gold_standard_no
        } else if (!c.gold_clause_no.empty() && !c.gold_standard_no.empty()) {
            gold_ref = c.gold_clause_no;
            gold_std = c.gold_standard_no;
        }
        const bool want_cite = !gold_ref.empty();
        const bool want_num  = !c.gold_values.empty();
        if (!want_cite && !want_num) continue;            // 无生成 gold（如纯覆盖题）→ 跳过，不调 LLM

        // 取缓存答案；未命中则调 answer_query 并缓存。
        std::string path = cache_path_for(answer_cache_dir, c.question);
        std::string answer = read_file(path);
        if (answer.empty()) {
            try {
                answer = answer_query(c.question, mv, embed, pg, syn, ds, collection, top_k, planner);
            } catch (const std::exception& e) {
                spdlog::warn("[gen-eval] answer_query 失败，跳过该条: {} ({})", c.question, e.what());
                continue;                                  // 该条不计分
            }
            if (!answer.empty()) write_file(answer_cache_dir, path, answer);
        }

        GenCaseResult cr;
        cr.question = c.question;
        if (want_cite) {
            cr.cite_scored = true;
            cr.cite_hit = citation_hit(answer, gold_std, gold_ref);
            rep.cite_scored++;
            if (cr.cite_hit) rep.cite_hits++;
        }
        if (want_num) {
            cr.value_gold = static_cast<int>(c.gold_values.size());
            cr.value_hits = count_value_hits(answer, c.gold_values);
            rep.value_gold_total += cr.value_gold;
            rep.value_hit_total  += cr.value_hits;
        }
        rep.results.push_back(cr);
    }
    return rep;
}
```

- [ ] **Step 4: 登记 `generation_eval.*` 到 app 工程（若 Task 2 未登记）**

确认 `rag2.0/rag2.0.vcxproj` 含：
```xml
    <ClCompile Include="..\src\eval\generation_eval.cpp" />
    <ClInclude Include="..\src\eval\generation_eval.h" />
```
及 `.filters` 对应 `<Filter>源文件\eval</Filter>` / `<Filter>头文件\eval</Filter>` 条目。**不**加入测试工程（infra-bound，无单测）。

- [ ] **Step 5: 构建（build-only，无新单测）+ 全量测试**

```powershell
& "D:\vs2022\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" rag2.0.sln /p:Configuration=Debug /p:Platform=x64 /m
.\rag2.0.tests\x64\Debug\rag2.0.tests.exe
```
Expected：0 编译/链接错误；全量绿（180，0 failed——本 Task 不加单测）。

- [ ] **Step 6: Commit**

```powershell
git add src/generate/prompt_builder.h src/generate/prompt_builder.cpp src/eval/generation_eval.h src/eval/generation_eval.cpp rag2.0/rag2.0.vcxproj rag2.0/rag2.0.vcxproj.filters
git commit -m @'
feat(eval): run_generation_eval 编排 + 答案磁盘缓存 + kAnswerPromptVersion

对带生成 gold 的用例调 answer_query(缓存包装)，打引用/数值指标；缓存键=normalize_question+prompt_version。
方法号取 stem(去年份)匹配；纯覆盖题跳过不调 LLM；单条失败 warn 不中断。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
'@
```

---

## Task 4: `main.cpp` 接线 `--gen` + 生成报告打印

**Files:**
- Modify: `src/main.cpp`

- [ ] **Step 1: `cmd_eval` 加 `bool gen` 参数并打印生成段**

`src/main.cpp` 顶部 include 区加：
```cpp
#include "eval/generation_eval.h"
```
`cmd_eval` 签名（当前 `static int cmd_eval(const Config& cfg, const std::string& dataset_path, int k, const std::string& planner_mode = "")`）改为：
```cpp
static int cmd_eval(const Config& cfg, const std::string& dataset_path, int k,
                    const std::string& planner_mode = "", bool gen = false) {
```
在打印「覆盖 coverage@k」的循环之后、`return 0;`（或 `catch`）之前加生成段：
```cpp
        if (gen) {
            if (cfg.deepseek_key.empty()) {
                spdlog::error("--gen 需要 RAG_DEEPSEEK_KEY，但未配置");
                return 1;
            }
            spdlog::info("[eval] generation eval on");
            GenerationReport grep = run_generation_eval(
                cases, mv, embed, pg, syn, ds, cfg.milvus_collection, k, planner,
                "data/answer_cache");
            std::cout << "\n--- 生成侧（--gen）---\n";
            for (const auto& r : grep.results) {
                if (r.cite_scored)
                    std::cout << "[cite] " << (r.cite_hit ? "OK " : "NG ")
                              << " " << r.question << "\n";
                if (r.value_gold > 0)
                    std::cout << "[num ] " << r.value_hits << "/" << r.value_gold
                              << "  " << r.question << "\n";
            }
            if (grep.cite_scored > 0)
                std::cout << "引用准确率: " << grep.cite_hits << "/" << grep.cite_scored << "\n";
            if (grep.value_gold_total > 0)
                std::cout << "数值准确率(逐值): " << grep.value_hit_total << "/"
                          << grep.value_gold_total << "\n";
        }
```
（`ds`/`planner`/`mv`/`embed`/`pg`/`syn` 在 `cmd_eval` 体内已构造——`ds` 见现 462-464 行、`planner` 见 464 行；若 `mv/embed/pg/syn` 的局部名不同，以函数体内实际变量名为准。）

- [ ] **Step 2: 派发块解析 `--gen`（任意位置）**

把 `if (cmd == "eval") { ... }` 块（现 558-565 行）替换为：
```cpp
    if (cmd == "eval") {
        bool gen = false;
        std::vector<std::string> pos;       // "eval" 之后的位置参数（剔除 --gen）
        for (int i = 2; i < argc; ++i) {
            std::string a = argv[i];
            if (a == "--gen") gen = true; else pos.push_back(a);
        }
        if (pos.empty()) {
            std::cout << "usage: rag2 eval <dataset.json> [k] [rule|llm|auto] [--gen]\n";
            return 1;
        }
        auto missing = cfg.missing_required();
        if (!missing.empty()) { for (auto& m : missing) spdlog::error("config.json 缺少必填项: {}", m); return 1; }
        int k = (pos.size() >= 2) ? std::max(1, std::atoi(pos[1].c_str())) : 20;
        std::string pmode = (pos.size() >= 3) ? pos[2] : "";
        return cmd_eval(cfg, pos[0], k, pmode, gen);
    }
```

- [ ] **Step 3: 构建 + 全量测试**

```powershell
& "D:\vs2022\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" rag2.0.sln /p:Configuration=Debug /p:Platform=x64 /m
.\rag2.0.tests\x64\Debug\rag2.0.tests.exe
```
Expected：0 编译错误；全量绿（180，0 failed）。

- [ ] **Step 4: 冒烟——不带 `--gen` 行为不变**

```powershell
$OutputEncoding=[Text.Encoding]::UTF8; [Console]::OutputEncoding=[Text.Encoding]::UTF8
.\rag2.0\x64\Debug\rag2.0.exe eval eval/dataset_seed.json 30 rule 2>&1 | Select-String -Pattern "point|MRR|coverage" | Out-String
```
Expected：与现状一致（点查 hit@30 5/5、MRR 1、coverage 22/22 14/14 5/9）；**无**生成段。

- [ ] **Step 5: Commit**

```powershell
git add src/main.cpp
git commit -m @'
feat(eval): wire --gen into cmd_eval (生成报告段)

eval 加 --gen(任意位置) opt-in；缺 RAG_DEEPSEEK_KEY 提前报错；检索段后追加引用/数值准确率段。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
'@
```

---

## Task 5: 数值题 gold_values 标注 + `--gen` 实跑验收

**Files:**
- Modify: `eval/dataset_seed.json`

> 本 Task 是数据标注 + 集成验收，不是写码。`gold_values` 的真实值由实现者从 `data/chunk_cache/*.json` 真相源导出（psql 不在 PATH，用 PowerShell `ConvertFrom-Json` 解析），用户抽查。

- [ ] **Step 1: 先跑一遍 `--gen` 看引用段（数值段此时为空）**

```powershell
$OutputEncoding=[Text.Encoding]::UTF8; [Console]::OutputEncoding=[Text.Encoding]::UTF8
.\rag2.0\x64\Debug\rag2.0.exe eval eval/dataset_seed.json 30 rule --gen 2>&1 | Select-String -Pattern "cite|num|引用|数值|生成侧" | Out-String
```
Expected：出现「--- 生成侧 ---」；5 条 `[cite]` 行（T0521/T0702/T0316/路基沉降/JTC5210）；`引用准确率: x/5`；暂无 `[num]` 行、无「数值准确率」（还没标 gold_values）。
人工抽查：随便挑 1-2 条 `[cite] OK/NG`，用 `rag2 query "<该问题>"` 看答案是否真引出了标准号+编号，确认判定无假阳/假阴。

- [ ] **Step 2: 从 chunk_cache 导数值事实，给 `eval/dataset_seed.json` 加数值题**

用 PowerShell 解析 `data/chunk_cache/*.json`，挑 ~10-15 个含明确限值/单位的条文（如针入度、压实度、荷载精度、温度范围等），为每条构造一个问题 + `gold_values`。在数组末尾追加形如：
```json
  ,{
    "question": "沥青针入度试验的标准试验温度和荷载是多少",
    "note": "numeric (沥青 JTG3410) — 数值准确率探针",
    "gold_values": ["25", "100", "5"]
  }
```
要点：
- `gold_values` 用**答案里会原样出现的最小字面**（如 `"25"` 而非 `"25℃"`，规避单位写法差异；单位另列一项如 `"℃"` 仅在确有把握时加）。
- 数值取自 chunk 正文真值，记下来源 chunk 便于抽查。
- 不必凑满 15；10 条覆盖 3-4 本标准即可。
- 这些用例无 `gold_method_no`/`gold_clause_no` → 只参与数值评分，不参与引用/检索点查评分。

- [ ] **Step 3: 跑 `--gen` 全量验收**

```powershell
$OutputEncoding=[Text.Encoding]::UTF8; [Console]::OutputEncoding=[Text.Encoding]::UTF8
.\rag2.0\x64\Debug\rag2.0.exe eval eval/dataset_seed.json 30 rule --gen 2>&1 | Select-String -Pattern "cite|num|引用|数值|生成侧|point|MRR|coverage" | Out-String
```
Expected（验收 spec §9）：
- 检索段不回归（点查 5/5、MRR 1、coverage 不变）。
- 生成段：5 条 `[cite]`、~10 条 `[num]`；打印「引用准确率: x/5」「数值准确率(逐值): y/z」。
- 人工抽查 2-3 条 `[num]`：用 `rag2 query` 看答案，核对命中数与肉眼一致（gold_values 字面是否真在答案里）。

- [ ] **Step 4: 重跑验确定性（答案缓存生效）**

```powershell
.\rag2.0\x64\Debug\rag2.0.exe eval eval/dataset_seed.json 30 rule --gen 2>&1 | Select-String -Pattern "引用|数值" | Out-String
```
Expected：两次「引用准确率」「数值准确率」**逐字一致**（`data/answer_cache` 命中，无新 LLM 调用）。可顺带确认 `data/answer_cache/` 下有 `.txt` 缓存文件。

- [ ] **Step 5: Commit**

```powershell
git add eval/dataset_seed.json
git commit -m @'
test(eval): 数值题 gold_values + 生成侧验收 (M4.2)

为 ~10 条数值题标 gold_values(取自 chunk_cache 真值)；eval --gen 跑通引用/数值两指标、重跑确定。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
'@
```

> 注：`eval/dataset_seed.json` 此前已有未提交的 8 条扩集（上个 session 的产物），本次提交一并带上。

---

## Self-Review

**1. Spec 覆盖（逐节对照）：**
- §2 范围（`--gen` opt-in、引用+数值、共用集、答案缓存、gold_values 我导）→ Task 1（字段/解析）+ Task 3（缓存/编排）+ Task 4（开关）+ Task 5（标注/验收）。
- §3.1 方案 A 非侵入（缓存包装复用 answer_query、键不绑上下文、双次检索）→ Task 3 `run_generation_eval`。§3.2 `kAnswerPromptVersion` → Task 3 Step 1。
- §4.1 `gold_values` → Task 1。§4.2 三个纯指标（双含/逐值/全角归一化）→ Task 2。§4.3 编排（gold_ref 选取、缺标准号只验 ref、单条失败 warn）→ Task 3。§4.4 `cmd_eval --gen` + 缺 key 报错 → Task 4。§4.5 工程登记 → Task 2 Step 4 + Task 3 Step 4。
- §5 数据流、§6 报告形态（[cite]/[num]/逐值口径）→ Task 4 Step 1 打印。
- §7 错误处理（单条失败/缺 key/缓存写失败/clause 缺标准号）→ Task 3 + Task 4。
- §8 测试（纯指标 TDD、parse 用例、编排 infra-bound 不单测）→ Task 1/2 单测 + Task 3 build-only。
- §9 验收（全量绿、--gen 跑通、引用抽查、数值抽查、重跑一致）→ Task 5 Step 1/3/4。

**2. Placeholder 扫描：** 无 TBD。唯一“留待执行期产出”的是 Task 5 的 `gold_values` 真实数值——这是数据标注（spec 明确“实现者从 chunk_cache 导”），已给出确切流程 + JSON 形状 + 抽查方法，非代码占位。

**3. 类型一致性：** `EvalCase.gold_values`(Task1) ↔ Task3 读取一致；`normalize_for_match`/`citation_hit(answer,std,ref)`/`count_value_hits(answer,values)`(Task2 头) ↔ Task3 调用一致；`GenCaseResult{cite_scored,cite_hit,value_gold,value_hits}`/`GenerationReport{cite_scored,cite_hits,value_gold_total,value_hit_total,results}`(Task3 头) ↔ Task4 打印字段一致；`run_generation_eval(...,answer_cache_dir)` 签名(Task3) ↔ Task4 调用实参一致；`kAnswerPromptVersion`(Task3 prompt_builder) ↔ Task3 缓存键使用一致；复用既有 `answer_query`/`normalize_question`/`cfg.deepseek_key`/`missing_required`。

**4. 已知限制（实现者须知）：**
- 答案缓存键不含检索上下文：改检索/RRF 后跑 `--gen` 前需清 `data/answer_cache`（spec §3.1）。
- 方法题引用只验方法号（无 gold_standard_no），是“复用现有 gold 不另标”的取舍（spec §2）；方法号按 stem 去年份匹配。
- `normalize_for_match` 只折叠全角数字与空白、ASCII 小写；不统一破折号/单位写法——`gold_values` 取“答案会原样出现的最小字面”来规避（Task 5 Step 2）。
