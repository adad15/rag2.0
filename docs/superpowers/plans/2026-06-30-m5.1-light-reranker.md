# M5.1 LightReranker Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 RRF 之后、pin 之前加一个可配置的 reranker 阶段，实现确定性 `LightReranker`（仅作用于普通题；以 RRF 序为基底做小步加/减分 + 同条款去重），用 rich eval 验证排序质量提升、零回归。

**Architecture:** 新增 `src/retrieve/reranker.{h,cpp}`：`RerankCandidate`（PG 文本回查补齐）、`IReranker` 接口（为 M5.2 留）、纯函数 `light_rerank`（打分+稳定排序+去重+截断）、纯函数 `assemble_rerank_candidates`。新增 `PgClient::get_chunks(ids)` 批量回查。`text_retrieve` 对**非列举题**在 light 模式下：RRF 融合到 `top_k*pool_mult` → 回查建池 → `light_rerank` → 现有 method/clause pin。列举题完全 bypass（保留现有 demote 链路）。

**Tech Stack:** C++20、MSBuild + vcpkg、libpqxx、doctest。沿用 [[rag2-build-setup]]、[[local-services]]。

**Spec:** [`docs/superpowers/specs/2026-06-30-m5.1-reranker-design.md`](../specs/2026-06-30-m5.1-reranker-design.md)（含 §4.1 brainstorm 决策）。**本 plan 只做 M5.1（off + light）；M5.2 model reranker 另起 plan。**

**构建/测试（PowerShell，项目根目录）：**
```powershell
& "D:\vs2022\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" rag2.0.sln /p:Configuration=Debug /p:Platform=x64 /m
.\rag2.0.tests\x64\Debug\rag2.0.tests.exe
```

**关键事实（源码勘察）：**
- `Candidate{standard_id,chunk_id,score,source}`（`src/retrieve/candidate.h`）。`source` 经 rrf 合并后含 `+`（如 `dense+bm25`/`dense+exact`）。
- `text_retrieve`（`src/retrieve/text_search.cpp:11`）现流程：plan→dense/bm25(+method exact)→`rrf_fuse(lists,60,fuse_k)`→列举 `demote_without_keyterms`→`pin_exact_clause`(method)→`pin_exact_clause`(clause)→return。**非列举 `fuse_k=top_k`（无大池）**。
- `pin_exact_clause`（`rrf.h`）：pinned 不在 fused 时**插入第一位**（所以硬 pin 不会被 rerank 截掉）。
- `QueryAnalysis`（`query_analysis.h`）：`intent`/`key_terms`/`method_no`/`clause_no`/`clean_text`。`key_terms` 在 rule planner 下普通题常为空（仅 list+白名单或 llm planner 才填）——light 的关键词加分在 rule 普通题多为 inert，主要靠去重+多路信号（已知，写进 §限制）。
- `RetrievalChunkRow`（`pg_client.h`）有 `title/path_text/atomic_text/context_text/bm25_text/clause_no/method_no/page_*`。`get_chunk` 每次新建 pqxx 连接（故需批量 `get_chunks`）。
- 配置：`config.cpp` from_map 用 `c.x = get(e,"RAG_X",default)`（见 :32 query_planner）。vcxproj 在 `..\src\query\query_planner.cpp`(:112)/`.h`(:165) 旁注册新文件。

**提交纪律：** 每次 pathspec 提交（`git add <files>` + `git commit -m "…" -- <files>`），绝不 `git add -A`/裸 commit。

---

## Task 1: reranker 纯层（RerankCandidate + light_rerank + assemble + IReranker）+ doctest

**Files:**
- Create: `src/retrieve/reranker.h`, `src/retrieve/reranker.cpp`, `tests/test_reranker.cpp`
- Modify: `rag2.0/rag2.0.vcxproj`(+`.filters`), `rag2.0.tests/rag2.0.tests.vcxproj`(+`.filters`)

- [ ] **Step 1: 写 `src/retrieve/reranker.h`**

```cpp
#pragma once
#include <string>
#include <vector>
#include "retrieve/candidate.h"
#include "db/pg_client.h"
#include "query/query_analysis.h"

// reranker 输入：RRF 候选 + PG 回查补齐的文本字段。不替代 RetrievalChunkRow。
struct RerankCandidate {
    Candidate base;
    std::string title;
    std::string path_text;
    std::string atomic_text;
    std::string context_text;
    std::string bm25_text;
    std::string clause_no;
    std::string method_no;
    int page_start = 0;
    int page_end = 0;
};

// 纯函数：按 fused 顺序把回查到的行拼成 RerankCandidate；回查不到的 chunk 跳过。
std::vector<RerankCandidate> assemble_rerank_candidates(
    const std::vector<Candidate>& fused,
    const std::vector<RetrievalChunkRow>& rows);

// 纯函数：LightReranker 核心。以 pool 的 RRF 顺序为基底做小步加分(稳定排序)，
// 再同条款(standard_id|clause_no)去重(每键最多 max_per_clause，超出降末尾)，截断 top_k。
// 返回 Candidate(base)，可直接接回 text_retrieve / pin。
std::vector<Candidate> light_rerank(const QueryAnalysis& qa,
                                    const std::vector<RerankCandidate>& pool,
                                    int top_k, int max_per_clause);

// M5.2 留口：轻量与大模型都实现它。M5.1 只有 LightReranker。
class IReranker {
public:
    virtual ~IReranker() = default;
    virtual std::vector<Candidate> rerank(const QueryAnalysis& qa,
                                          const std::vector<RerankCandidate>& pool,
                                          int top_k) = 0;
};

class LightReranker : public IReranker {
public:
    explicit LightReranker(int max_per_clause = 2) : max_per_clause_(max_per_clause) {}
    std::vector<Candidate> rerank(const QueryAnalysis& qa,
                                  const std::vector<RerankCandidate>& pool,
                                  int top_k) override {
        return light_rerank(qa, pool, top_k, max_per_clause_);
    }
private:
    int max_per_clause_;
};
```

- [ ] **Step 2: 写 `tests/test_reranker.cpp`**

```cpp
#include <doctest/doctest.h>
#include "retrieve/reranker.h"

static RerankCandidate mk(const std::string& cid, const std::string& src, int rrf_rank,
                          const std::string& clause = "", const std::string& method = "",
                          const std::string& title = "", const std::string& standard = "s1") {
    RerankCandidate c;
    c.base.standard_id = standard;
    c.base.chunk_id = cid;
    c.base.source = src;
    c.base.score = 1.0f / (rrf_rank + 1);  // 模拟 RRF 序（idx 决定输入顺序，见各用例）
    c.clause_no = clause;
    c.method_no = method;
    c.title = title;
    return c;
}

TEST_CASE("light_rerank: no signals keeps RRF order") {
    QueryAnalysis qa;  // 无 key_terms/method/clause
    std::vector<RerankCandidate> pool = {mk("a","dense",0), mk("b","bm25",1), mk("c","dense",2)};
    auto out = light_rerank(qa, pool, 10, 2);
    REQUIRE(out.size() == 3);
    CHECK(out[0].chunk_id == "a");
    CHECK(out[1].chunk_id == "b");
    CHECK(out[2].chunk_id == "c");
}

TEST_CASE("light_rerank: key_term hit lifts a lower candidate") {
    QueryAnalysis qa; qa.key_terms = {"安定性"};
    std::vector<RerankCandidate> pool = {
        mk("a","dense",0), mk("b","dense",1)};
    pool[1].title = "安定性";   // b 命中关键词 → 上提到 a 前
    auto out = light_rerank(qa, pool, 10, 2);
    CHECK(out[0].chunk_id == "b");
    CHECK(out[1].chunk_id == "a");
}

TEST_CASE("light_rerank: multi-source (+) gives small lift") {
    QueryAnalysis qa;
    std::vector<RerankCandidate> pool = {mk("a","dense",0), mk("b","dense+bm25",1)};
    auto out = light_rerank(qa, pool, 10, 2);
    CHECK(out[0].chunk_id == "b");   // 多路融合 +1.5 > 单路
}

TEST_CASE("light_rerank: method_no match lifts strongly") {
    QueryAnalysis qa; qa.method_no = "T0302";
    std::vector<RerankCandidate> pool = {
        mk("a","dense",0), mk("b","dense",1,"","T0302")};
    auto out = light_rerank(qa, pool, 10, 2);
    CHECK(out[0].chunk_id == "b");
}

TEST_CASE("light_rerank: clause dedup demotes 3rd same-clause to end") {
    QueryAnalysis qa;
    std::vector<RerankCandidate> pool = {
        mk("a","dense",0,"5.1"), mk("b","dense",1,"5.1"),
        mk("c","dense",2,"5.1"), mk("d","dense",3,"9.9")};
    auto out = light_rerank(qa, qa.key_terms.empty()? pool: pool, 10, 2);
    // a,b 同条款保留(<=2)，c 超额降末尾，d 不同条款正常
    REQUIRE(out.size() == 4);
    CHECK(out[0].chunk_id == "a");
    CHECK(out[1].chunk_id == "b");
    CHECK(out[2].chunk_id == "d");
    CHECK(out[3].chunk_id == "c");   // 超额者降到末尾
}

TEST_CASE("light_rerank: stable on equal adjustment (RRF order) + truncate") {
    QueryAnalysis qa;
    std::vector<RerankCandidate> pool = {mk("a","dense",0), mk("b","dense",1), mk("c","dense",2)};
    auto out = light_rerank(qa, pool, 2, 2);
    REQUIRE(out.size() == 2);
    CHECK(out[0].chunk_id == "a");
    CHECK(out[1].chunk_id == "b");
}

TEST_CASE("assemble_rerank_candidates preserves fused order and skips missing") {
    std::vector<Candidate> fused;
    Candidate c1; c1.chunk_id="x"; c1.standard_id="s"; fused.push_back(c1);
    Candidate c2; c2.chunk_id="missing"; fused.push_back(c2);
    Candidate c3; c3.chunk_id="y"; fused.push_back(c3);
    std::vector<RetrievalChunkRow> rows;
    RetrievalChunkRow rx; rx.chunk_id="x"; rx.title="标题X"; rx.clause_no="1.1"; rows.push_back(rx);
    RetrievalChunkRow ry; ry.chunk_id="y"; ry.bm25_text="bm25Y"; rows.push_back(ry);
    auto out = assemble_rerank_candidates(fused, rows);
    REQUIRE(out.size() == 2);            // missing 跳过
    CHECK(out[0].base.chunk_id == "x");
    CHECK(out[0].title == "标题X");
    CHECK(out[1].base.chunk_id == "y");
    CHECK(out[1].bm25_text == "bm25Y");
}
```

- [ ] **Step 3: 写 `src/retrieve/reranker.cpp`**

```cpp
#include "retrieve/reranker.h"
#include <algorithm>
#include <map>

std::vector<RerankCandidate> assemble_rerank_candidates(
    const std::vector<Candidate>& fused, const std::vector<RetrievalChunkRow>& rows) {
    std::map<std::string, const RetrievalChunkRow*> by_id;
    for (const auto& r : rows) by_id[r.chunk_id] = &r;
    std::vector<RerankCandidate> out;
    for (const auto& c : fused) {
        auto it = by_id.find(c.chunk_id);
        if (it == by_id.end()) continue;
        const RetrievalChunkRow& r = *it->second;
        RerankCandidate rc;
        rc.base = c;
        rc.title = r.title; rc.path_text = r.path_text; rc.atomic_text = r.atomic_text;
        rc.context_text = r.context_text; rc.bm25_text = r.bm25_text;
        rc.clause_no = r.clause_no; rc.method_no = r.method_no;
        rc.page_start = r.page_start; rc.page_end = r.page_end;
        out.push_back(std::move(rc));
    }
    return out;
}

std::vector<Candidate> light_rerank(const QueryAnalysis& qa,
                                    const std::vector<RerankCandidate>& pool,
                                    int top_k, int max_per_clause) {
    struct Scored { size_t idx; double adj; };
    std::vector<Scored> sc;
    sc.reserve(pool.size());
    for (size_t i = 0; i < pool.size(); ++i) {
        const RerankCandidate& c = pool[i];
        double adj = 0.0;
        if (c.base.source.find('+') != std::string::npos) adj += 1.5;   // 多路融合
        if (!qa.method_no.empty() && c.method_no == qa.method_no) adj += 3.0;
        if (!qa.clause_no.empty() && c.clause_no == qa.clause_no) adj += 3.0;
        int kt = 0;                                                      // 关键词命中，每个 +1 封顶 +3
        for (const auto& t : qa.key_terms) {
            if (t.empty()) continue;
            if (c.title.find(t) != std::string::npos || c.path_text.find(t) != std::string::npos ||
                c.atomic_text.find(t) != std::string::npos || c.context_text.find(t) != std::string::npos ||
                c.bm25_text.find(t) != std::string::npos) {
                if (++kt >= 3) break;
            }
        }
        adj += static_cast<double>(kt);
        sc.push_back({i, adj});
    }
    // 稳定排序：adj 降序，相等保留输入(RRF)顺序
    std::stable_sort(sc.begin(), sc.end(),
                     [](const Scored& a, const Scored& b) { return a.adj > b.adj; });

    // 同条款去重：clause_no 非空时每 (standard_id|clause_no) 最多 max_per_clause，超额降末尾
    std::vector<Candidate> kept, overflow;
    std::map<std::string, int> clause_count;
    for (const auto& s : sc) {
        const RerankCandidate& c = pool[s.idx];
        if (!c.clause_no.empty()) {
            std::string key = c.base.standard_id + "|" + c.clause_no;
            if (++clause_count[key] > max_per_clause) { overflow.push_back(c.base); continue; }
        }
        kept.push_back(c.base);
    }
    kept.insert(kept.end(), overflow.begin(), overflow.end());
    if (static_cast<int>(kept.size()) > top_k) kept.resize(top_k);
    return kept;
}
```

- [ ] **Step 4: 登记新文件**

`rag2.0/rag2.0.vcxproj`：在 `<ClCompile Include="..\src\query\query_planner.cpp" />` 同类块里加 `<ClCompile Include="..\src\retrieve\reranker.cpp" />`；在 `<ClInclude ...query_planner.h />` 旁加 `<ClInclude Include="..\src\retrieve\reranker.h" />`。
`rag2.0/rag2.0.vcxproj.filters`：加
```xml
    <ClCompile Include="..\src\retrieve\reranker.cpp"><Filter>源文件\retrieve</Filter></ClCompile>
    <ClInclude Include="..\src\retrieve\reranker.h"><Filter>头文件\retrieve</Filter></ClInclude>
```
（找不到锚点就 `Select-String query_planner <文件>` 找同类条目就近加。）
`rag2.0.tests/rag2.0.tests.vcxproj`：加
```xml
    <ClCompile Include="..\src\retrieve\reranker.cpp" />
    <ClCompile Include="..\tests\test_reranker.cpp" />
```
`rag2.0.tests/rag2.0.tests.vcxproj.filters`：加
```xml
    <ClCompile Include="..\src\retrieve\reranker.cpp"><Filter>被测源码</Filter></ClCompile>
    <ClCompile Include="..\tests\test_reranker.cpp"><Filter>测试</Filter></ClCompile>
```

- [ ] **Step 5: 构建 + 运行 reranker 测试 + 全量**
```powershell
& "D:\vs2022\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" rag2.0.sln /p:Configuration=Debug /p:Platform=x64 /m
.\rag2.0.tests\x64\Debug\rag2.0.tests.exe --test-case="*light_rerank*,*assemble_rerank*"
.\rag2.0.tests\x64\Debug\rag2.0.tests.exe
```
Expected：reranker 用例全 PASS；全量 0 failed。

- [ ] **Step 6: Commit**
```powershell
git add src/retrieve/reranker.h src/retrieve/reranker.cpp tests/test_reranker.cpp rag2.0/rag2.0.vcxproj rag2.0/rag2.0.vcxproj.filters rag2.0.tests/rag2.0.tests.vcxproj rag2.0.tests/rag2.0.tests.vcxproj.filters
git commit -m @'
feat(rerank): LightReranker 纯层 (RerankCandidate/light_rerank/assemble + IReranker)

RRF 序基底小步加分(多路/method/clause/key_term)+稳定排序+同条款去重截断;纯函数 doctest。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
'@ -- src/retrieve/reranker.h src/retrieve/reranker.cpp tests/test_reranker.cpp rag2.0/rag2.0.vcxproj rag2.0/rag2.0.vcxproj.filters rag2.0.tests/rag2.0.tests.vcxproj rag2.0.tests/rag2.0.tests.vcxproj.filters
```

---

## Task 2: `PgClient::get_chunks` 批量回查 + `build_rerank_candidates`

**Files:** Modify `src/db/pg_client.h`, `src/db/pg_client.cpp`, `src/retrieve/reranker.h`, `src/retrieve/reranker.cpp`

- [ ] **Step 1: 声明（`src/db/pg_client.h`）**

在 `std::optional<RetrievalChunkRow> get_chunk(const std::string& chunk_id);` 之后加：
```cpp
    // 批量回查：一次取多 chunk（动态 IN，避免逐个 get_chunk 各开连接）。缺失的 id 不出现在结果里。
    std::vector<RetrievalChunkRow> get_chunks(const std::vector<std::string>& chunk_ids);
```

- [ ] **Step 2: 实现（`src/db/pg_client.cpp`，紧接 `get_chunk` 之后）**

```cpp
std::vector<RetrievalChunkRow> PgClient::get_chunks(const std::vector<std::string>& chunk_ids) {
    std::vector<RetrievalChunkRow> out;
    if (chunk_ids.empty()) return out;
    pqxx::connection cn(conninfo_);
    pqxx::work tx(cn);
    std::string sql =
        "SELECT chunk_id,node_id,standard_id,COALESCE(chunk_type,''),"
        "COALESCE(clause_no,''),COALESCE(method_no,''),COALESCE(title,''),"
        "COALESCE(path_text,''),COALESCE(atomic_text,''),COALESCE(embedding_text,''),"
        "COALESCE(context_text,''),COALESCE(captions::text,'[]'),COALESCE(formulas::text,'[]'),"
        "COALESCE(page_start,0),COALESCE(page_end,0),COALESCE(has_table,FALSE),"
        "COALESCE(has_formula,FALSE),COALESCE(has_figure,FALSE),COALESCE(suspect,''),"
        "COALESCE(bm25_text,'') FROM retrieval_chunks WHERE chunk_id IN (";
    pqxx::params p;
    for (size_t i = 0; i < chunk_ids.size(); ++i) {
        if (i) sql += ",";
        sql += "$" + std::to_string(i + 1);
        p.append(chunk_ids[i]);
    }
    sql += ")";
    for (const auto& row : tx.exec(sql, p)) {
        RetrievalChunkRow c;
        c.chunk_id = row[0].c_str(); c.node_id = row[1].c_str();
        c.standard_id = row[2].c_str(); c.chunk_type = row[3].c_str();
        c.clause_no = row[4].c_str(); c.method_no = row[5].c_str();
        c.title = row[6].c_str(); c.path_text = row[7].c_str();
        c.atomic_text = row[8].c_str(); c.embedding_text = row[9].c_str();
        c.context_text = row[10].c_str(); c.captions_json = row[11].c_str();
        c.formulas_json = row[12].c_str(); c.page_start = row[13].as<int>();
        c.page_end = row[14].as<int>(); c.has_table = row[15].as<bool>();
        c.has_formula = row[16].as<bool>(); c.has_figure = row[17].as<bool>();
        c.suspect = row[18].c_str(); c.bm25_text = row[19].c_str();
        out.push_back(std::move(c));
    }
    return out;
}
```
（字段顺序/索引与现有 `get_chunk` 完全一致，bm25_text=row[19]。）

- [ ] **Step 3: `build_rerank_candidates`（IO 包装，`reranker.h` 声明 + `reranker.cpp` 实现）**

`reranker.h` 在 `assemble_rerank_candidates` 声明后加：
```cpp
// IO：按 fused 的 chunk_id 批量回查 PG，拼 RerankCandidate（保序、缺失跳过）。
std::vector<RerankCandidate> build_rerank_candidates(const std::vector<Candidate>& fused, PgClient& pg);
```
`reranker.cpp` 末尾加：
```cpp
std::vector<RerankCandidate> build_rerank_candidates(const std::vector<Candidate>& fused, PgClient& pg) {
    std::vector<std::string> ids;
    ids.reserve(fused.size());
    for (const auto& c : fused) ids.push_back(c.chunk_id);
    return assemble_rerank_candidates(fused, pg.get_chunks(ids));
}
```

- [ ] **Step 4: 构建 + 全量测试**
```powershell
& "D:\vs2022\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" rag2.0.sln /p:Configuration=Debug /p:Platform=x64 /m
.\rag2.0.tests\x64\Debug\rag2.0.tests.exe
```
Expected：0 编译错误；全量 0 failed。（`get_chunks`/`build_rerank_candidates` 走真 PG，无单测；纯层 `assemble_rerank_candidates` 已在 Task 1 覆盖；端到端由 Task 4 集成验证。）

- [ ] **Step 5: Commit**
```powershell
git add src/db/pg_client.h src/db/pg_client.cpp src/retrieve/reranker.h src/retrieve/reranker.cpp
git commit -m @'
feat(rerank): PgClient::get_chunks 批量回查 + build_rerank_candidates

动态 IN 一次取多 chunk(避免逐个 get_chunk 各开连接);build_rerank_candidates 保序拼池。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
'@ -- src/db/pg_client.h src/db/pg_client.cpp src/retrieve/reranker.h src/retrieve/reranker.cpp
```

---

## Task 3: 配置 + text_retrieve 接线 + 透传

**Files:** Modify `src/rag_config.h`, `src/config.cpp`, `config.example.json`, `src/retrieve/reranker.h`, `src/retrieve/text_search.h`, `src/retrieve/text_search.cpp`, `src/generate/answer_pipeline.h`, `src/generate/answer_pipeline.cpp`, `src/eval/eval_runner.h`, `src/eval/eval_runner.cpp`, `src/main.cpp`

- [ ] **Step 1: `RerankParams`（`src/retrieve/reranker.h`，文件顶部 struct 区）**

在 `RerankCandidate` 定义前加：
```cpp
struct RerankParams {
    std::string mode = "off";   // off | light（model/hybrid 留给 M5.2）
    int pool_mult = 4;          // 非列举题候选池 = top_k * pool_mult
    int max_per_clause = 2;
};
```

- [ ] **Step 2: 配置字段（`src/rag_config.h` + `src/config.cpp` + `config.example.json`）**

`rag_config.h` 在 `std::string query_planner;` 后加：
```cpp
    std::string rerank_mode;          // off | light（默认 off）
    int         rerank_pool_mult = 4;
    int         rerank_max_per_clause = 2;
```
`config.cpp` from_map 在 `c.query_planner = get(e, "RAG_QUERY_PLANNER", "auto");` 后加：
```cpp
    c.rerank_mode          = get(e, "RAG_RERANK_MODE", "off");
    c.rerank_pool_mult     = std::stoi(get(e, "RAG_RERANK_POOL_MULT", "4"));
    c.rerank_max_per_clause = std::stoi(get(e, "RAG_RERANK_MAX_PER_CLAUSE", "2"));
```
`config.example.json` 在 `"RAG_QUERY_PLANNER": "auto"` 后加（注意逗号）：
```json
  ,"RAG_RERANK_MODE": "off"
  ,"RAG_RERANK_POOL_MULT": "4"
  ,"RAG_RERANK_MAX_PER_CLAUSE": "2"
```
> 默认 `off`：合并不改线上行为；Task 4 eval 证明 light 净收益后再改默认。

- [ ] **Step 3: `text_retrieve` 接收 RerankParams 并接线（`src/retrieve/text_search.h` / `.cpp`）**

`text_search.h`：`#include "retrieve/reranker.h"`；签名末尾加 `, const RerankParams& rerank`。
`text_search.cpp`：`#include "retrieve/reranker.h"`；把现有融合/截断段：
```cpp
    const int fuse_k = list_recall ? 1000000 : top_k;
    std::vector<Candidate> fused = rrf_fuse(lists, /*k=*/60, fuse_k);
    if (list_recall)
        fused = demote_without_keyterms(fused, key_hit_ids, top_k);
```
替换为：
```cpp
    const bool do_rerank = (rerank.mode == "light") && !list_recall;
    const int fuse_k = list_recall ? 1000000
                       : (do_rerank ? top_k * std::max(1, rerank.pool_mult) : top_k);
    std::vector<Candidate> fused = rrf_fuse(lists, /*k=*/60, fuse_k);
    if (list_recall) {
        fused = demote_without_keyterms(fused, key_hit_ids, top_k);
    } else if (do_rerank) {
        std::vector<RerankCandidate> pool = build_rerank_candidates(fused, pg);
        fused = light_rerank(qa, pool, top_k, rerank.max_per_clause);
    }
```
（`<algorithm>` 已随其他头可用；若缺补 `#include <algorithm>`。method/clause pin 段保持不动——pin 在最后，且 `pin_exact_clause` 对不在 fused 的 pin 会插首位，硬命中不丢。）

- [ ] **Step 4: `answer_query` 透传（`src/generate/answer_pipeline.h` / `.cpp`）**

`answer_pipeline.h`：`#include "retrieve/reranker.h"`；`answer_query` 签名末尾加 `, const RerankParams& rerank`。
`answer_pipeline.cpp`：`text_retrieve(...)` 调用末尾加 `, rerank`；定义签名同步加 `const RerankParams& rerank`。

- [ ] **Step 5: `run_eval` / `run_rich_eval` 透传（`src/eval/eval_runner.h` / `.cpp`）**

`eval_runner.h`：`#include "retrieve/reranker.h"`；`run_eval` 与 `run_rich_eval` 签名末尾各加 `, const RerankParams& rerank`。
`eval_runner.cpp`：两处 `text_retrieve(...)` 调用末尾各加 `, rerank`；两函数定义签名同步。

- [ ] **Step 6: `src/main.cpp` 构造 RerankParams 并接入三命令**

顶部 `#include "retrieve/reranker.h"`。加工厂：
```cpp
static RerankParams make_rerank(const Config& cfg) {
    RerankParams r;
    r.mode = cfg.rerank_mode.empty() ? "off" : cfg.rerank_mode;
    r.pool_mult = cfg.rerank_pool_mult;
    r.max_per_clause = cfg.rerank_max_per_clause;
    return r;
}
```
- **cmd_query**：`answer_query(question, mv, embed, pg, syn, ds, cfg.milvus_collection, 5, planner)` 末尾加 `, make_rerank(cfg)`。
- **cmd_retrievecheck**：`text_retrieve(question, mv, embed, pg, syn, cfg.milvus_collection, k*4, k, planner)` 末尾加 `, make_rerank(cfg)`。
- **cmd_eval**：`run_eval(...)` 与（rich 段）`run_rich_eval(...)` 调用末尾各加 `, make_rerank(cfg)`。
（spdlog 记一行 `[rerank] mode={} pool_mult={} max_per_clause={}` 便于 eval 对照。）

- [ ] **Step 7: 构建 + 全量测试**
```powershell
& "D:\vs2022\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" rag2.0.sln /p:Configuration=Debug /p:Platform=x64 /m
.\rag2.0.tests\x64\Debug\rag2.0.tests.exe
```
Expected：0 编译错误；全量 0 failed（默认 off → 行为与现状一致）。

- [ ] **Step 8: Commit**
```powershell
git add src/rag_config.h src/config.cpp config.example.json src/retrieve/reranker.h src/retrieve/text_search.h src/retrieve/text_search.cpp src/generate/answer_pipeline.h src/generate/answer_pipeline.cpp src/eval/eval_runner.h src/eval/eval_runner.cpp src/main.cpp
git commit -m @'
feat(rerank): RAG_RERANK_* 配置 + text_retrieve 接入 LightReranker(仅非列举题)

off|light 开关(默认 off);light 时非列举题 RRF 融合到 top_k*pool_mult→回查建池→light_rerank;
列举题 bypass;method/clause pin 不变。透传 answer_query/run_eval/run_rich_eval。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
'@ -- src/rag_config.h src/config.cpp config.example.json src/retrieve/reranker.h src/retrieve/text_search.h src/retrieve/text_search.cpp src/generate/answer_pipeline.h src/generate/answer_pipeline.cpp src/eval/eval_runner.h src/eval/eval_runner.cpp src/main.cpp
```

---

## Task 4: off-vs-light rich eval 验收 + 默认决策 + README/记忆（内联，需 live 服务）

**Files:** 可能改 `config.example.json`/`src/config.cpp`（若 light 胜出改默认）、`README.md`。需 Milvus + PG + embedding 在线。

- [ ] **Step 1: 确认服务在线**（见 [[local-services]]）
```powershell
docker ps --filter "name=milvus-standalone" --format "{{.Names}} {{.Status}}"
python -c "import requests;print(requests.post('http://localhost:19530/v2/vectordb/collections/list',headers={'Authorization':'Bearer root:Milvus'},json={},timeout=8).json().get('code'))"
```
Expected：healthy、code 0。

- [ ] **Step 2: 跑 off 基线**
临时设 `config.json` 的 `RAG_RERANK_MODE=off`（或确认默认即 off），跑：
```powershell
$OutputEncoding=[Text.Encoding]::UTF8; [Console]::OutputEncoding=[Text.Encoding]::UTF8
.\rag2.0\x64\Debug\rag2.0.exe eval eval/retrieval_questions_100.annotated.json 30 rule --rich 2>&1 | Tee-Object logs/eval_rerank_off.txt | Select-String -Pattern "Group Recall@20|Complete@20|nDCG@20|Redundancy@20|Distractor-before-gold|rq-081"
```

- [ ] **Step 3: 跑 light**
把 `config.json` 的 `RAG_RERANK_MODE=light`，跑：
```powershell
.\rag2.0\x64\Debug\rag2.0.exe eval eval/retrieval_questions_100.annotated.json 30 rule --rich 2>&1 | Tee-Object logs/eval_rerank_light.txt | Select-String -Pattern "Group Recall@20|Complete@20|nDCG@20|Redundancy@20|Distractor-before-gold|rq-081"
```

- [ ] **Step 4: 对照验收（spec §11.3 / §12）**
对比 off vs light：
- `Group Recall@20` 不明显下降（≥ off - 0.005）；
- `Complete@20` 不明显下降（≥ off - 0.01）；
- `nDCG@20` 提升或不退步（≥ off - 0.005）；
- `Redundancy@20` 下降（< off）；
- `Distractor-before-gold` 不升高（≤ off + 0.01）；
- `rq-081` 仍前排命中（不回退）。
把两组数字记进 `logs/`（已 gitignore）+ 下面 README 一行。

- [ ] **Step 5: 默认决策**
- 若 light 满足全部门槛（尤其 Redundancy 降、nDCG 不退、覆盖不降）：把默认改为 light——`config.cpp` `RAG_RERANK_MODE` 默认 `"light"` + `config.example.json` 改 `"light"`，重新构建跑一次确认。
- 若任一门槛破：默认保持 off，记录哪条破线 + 触发题（供调权重/下一刀），**不在本刀硬调权重**。

- [ ] **Step 6: README + 记忆**
README 增/改一行（按实测）：
```markdown
> M5.1 LightReranker（rerank.mode=off|light，仅普通题；RRF 序小步加分 + 同条款去重）：light vs off —— nDCG@20 <x>→<y>、Redundancy@20 <a>→<b>、覆盖不降、rq-081 保持前排。
```
并在用法段补 `RAG_RERANK_MODE` 说明。更新 [[bm25-suspected-broken]] 记忆：M5.1 落地 + off/light 实测结论 + 默认值。

- [ ] **Step 7: Commit（若有 README/默认改动，pathspec）**
```powershell
git add README.md src/config.cpp config.example.json
git commit -m @'
docs(rerank): M5.1 off-vs-light eval 结论 + README/默认<同步>

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
'@ -- README.md src/config.cpp config.example.json
```

---

## Self-Review

**1. Spec 覆盖：**
- §4 架构（RRF→builder→light→policy→pin）→ Task 1（light+policy 合一 light_rerank）+ Task 3（接线，pin 不动在最后）。§4.1 决策①范围(仅普通题/列举 bypass)→ Task 3 `do_rerank = light && !list_recall`；②打分形态(RRF 基底小步+稳定)→ Task 1 light_rerank(adj+stable_sort)；③批量回查→ Task 2 get_chunks；④默认由 eval 定→ Task 3 默认 off + Task 4 决策；⑤pin 顺序→ Task 3(pin 段保留在最后)。
- §5.1 RerankCandidate→Task1。§5.2 RerankCandidateBuilder→Task2(build_rerank_candidates,回查不到跳过+不写库)。§5.3 IReranker→Task1。§5.4 LightReranker 加分/降权信号→Task1 light_rerank(多路/method/clause/key_term 加分；同条款去重)。§5.5 RerankPolicy(保护编号/控重复/稳定截断)→Task1(去重+稳定截断)+Task3(method/clause pin 保留)。
- §6 配置(off/light、candidate_pool_k=top_k*4、max_per_clause=2)→Task3。§7 数据流→Task3 顺序。§8 key_terms(列举过滤不变/普通软加分)→Task3(列举 bypass)+Task1(key_term 加分不过滤)。§10 错误处理(回查不到跳过、off 走旧链路)→Task1/Task3。§11 测试(单元+集成+off/light eval)→Task1 doctest + Task4 eval。§12 验收→Task4。§13 风险(小步/普通只软加分/同条款不跨条款压/pool_mult 默认4)→Task1/Task3 默认值。
- §9 M5.2、§5.5 policy 完全独立化、model/hybrid 实现 → **本 plan 不做**（spec 明确 M5.2 另起）。

**2. Placeholder 扫描：** 无 TBD/TODO；纯函数与配置/接线给完整代码；Task4 为 live 验收（命令确切，数字以实跑为准属正常）。

**3. 类型一致性：** `RerankCandidate`/`RerankParams`/`light_rerank(qa,pool,top_k,max_per_clause)`/`assemble_rerank_candidates(fused,rows)`/`build_rerank_candidates(fused,pg)`/`PgClient::get_chunks(ids)`/`IReranker::rerank`/`LightReranker` 跨 Task 1-3 定义=调用一致；`text_retrieve(...,const RerankParams&)`/`answer_query(...,const RerankParams&)`/`run_eval`/`run_rich_eval` 透传一致；`make_rerank(cfg)` 读 `cfg.rerank_mode/pool_mult/max_per_clause`。get_chunks 字段索引与 get_chunk 同(bm25_text=row[19])。

**4. 已知限制：** ① rule planner 下普通题 `key_terms` 常空 → light 的关键词加分 inert，主要靠多路+同条款去重(redundancy↓)与 method/clause(与硬 pin 重叠)；关键词价值需 llm planner，Task4 可选再跑 llm 对照。② `light_rerank` 的“核心词”只取 `qa.key_terms`，不另做 clean_text 分词(避免引 jieba/scope 膨胀)。③ get_chunks 用动态 IN(pool_k≈120 个 $n 参数)，远小于 PG 限制，安全。
