# M5.2 ModelReranker Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给文本检索链路加一个云端模型重排层（硅基流动 `Qwen/Qwen3-Reranker-8B`），支持 `RAG_RERANK_MODE=model|hybrid`，成功用模型序、失败确定性回退，rerank 打分落盘缓存，用 rich eval 四模式对比决定默认值。

**Architecture:** 新增 `src/retrieve/rerank_api_client.{h,cpp}`（纯请求体构造 + 响应解析 + `RerankApiClient::score` 网络调用）。`src/retrieve/reranker.{h,cpp}` 里：抽出共用后处理 `finalize_rerank`（clause 去重+截断，`light_rerank` 改调它）、纯函数 `compose_rerank_document`、纯函数 `assemble_model_ranking`（模型输出排序规则）、`ModelReranker`（IReranker：组文档→查缓存→调 `score_call`→成功 assemble、失败走注入的兜底 IReranker）、`RrfPassthrough`。`RerankParams` 扩容（instruction/cache_dir/model/score_call），沿用 M5.1 既有的"RerankParams 流经四个入口"不改签名。`text_retrieve` 按 mode 造 IReranker 走多态。

**Tech Stack:** C++20、MSBuild + vcpkg、nlohmann/json、libpqxx、doctest。沿用 [[rag2-build-setup]]、[[local-services]]。

**Spec:** [`docs/superpowers/specs/2026-07-01-m5.2-model-reranker-design.md`](../specs/2026-07-01-m5.2-model-reranker-design.md)。

**构建/测试（PowerShell，项目根目录）：**
```powershell
& "D:\vs2022\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" rag2.0.sln /p:Configuration=Debug /p:Platform=x64 /m
.\rag2.0.tests\x64\Debug\rag2.0.tests.exe
```
单测跑某个 case：`.\rag2.0.tests\x64\Debug\rag2.0.tests.exe -tc="<case 名>"`。

**关键事实（源码勘察，2026-07-01）：**
- `Candidate{standard_id,chunk_id,score,source}`（`src/retrieve/candidate.h`）。`source` 经 rrf 合并含 `+`（`dense+bm25`）。
- `RerankCandidate{base,title,path_text,atomic_text,context_text,bm25_text,clause_no,method_no,page_*}`（`reranker.h`）。
- 现有 `light_rerank`（`reranker.cpp:25`）内部含 clause 去重（:52-64），本 plan Task 4 抽成 `finalize_rerank`；现有 `tests/test_reranker.cpp` 的 `clause dedup`/`stable+truncate` 用例是它的回归护栏，重构后必须仍绿。
- `text_retrieve`（`text_search.cpp:11`）现流程：plan→dense/bm25(+method exact)→`rrf_fuse(lists,60,fuse_k)`→列举 `demote_without_keyterms`／非列举 `do_rerank`（:71 `mode=="light"`）→`build_rerank_candidates`→`light_rerank_with_fallback`→`pin_exact_clause`(method)→`pin_exact_clause`(clause)。**非列举 do_rerank 时 `fuse_k=top_k*pool_mult`**。
- `QueryAnalysis`（`query_analysis.h`）有 `intent/key_terms/method_no/clause_no/clean_text/dense_text/sparse_text`。
- 配置：`Config`（`src/rag_config.h`）+ `Config::from_map`（`src/config.cpp`），`c.x = get(e,"RAG_X",default)`；`deepseek_key` 在 :26 先设好（rerank_key 回退可用它）。`make_rerank`（`src/main.cpp:73`）把 `Config`→`RerankParams`。
- 缓存范式：`generation_eval.cpp:14-32` 的 `cache_path_for`(hash→`%016llx` 文件名)/`read_file`/`write_file`（`std::filesystem::create_directories` + 写失败非致命）。`normalize_question`（`query_planner.h:27`，纯函数，query_planner.cpp 已在两个工程）。
- UTF-8 安全截断：`text_utf8::truncate(s,max_chars)`（`src/util/text_utf8.h`，按字符数截断+省略号，纯 inline）。本 plan Task 3 给它加一个 `char_count`。
- HTTP：`http::post_json(base_url,path,body,headers,read_timeout_sec=120)`→`http::Response{status,body,error,ok()}`（`src/http/http_client.h`）。参考客户端 `cloud_embedding.cpp`（build body / parse / 重试）、`deepseek_client.cpp`。
- 工程注册：源文件进 `rag2.0/rag2.0.vcxproj`（`ClCompile`/`ClInclude`）**和** `rag2.0.tests/rag2.0.tests.vcxproj`（tests 工程直接编 `..\src\...`，见 reranker.cpp :150）；各自 `.filters` 同步一份。tests 工程已含 `http_client.cpp`(:76)/`pg_client.cpp`(:77)/`query_planner.cpp`(:131)——链接无忧。

**提交纪律：** 每次 pathspec 提交（`git add <files>` 后 `git commit -m "…" -- <files>`），绝不 `git add -A`/裸 `git commit`。本仓库工作区有"勿擅动"的未提交文件，务必只提本任务碰的文件。

---

## Task 1: 配置项（RAG_RERANK_BASE_URL/PATH/MODEL/KEY/TIMEOUT/INSTRUCTION）

**Files:**
- Modify: `src/rag_config.h`, `src/config.cpp`, `config.example.json`
- Test: `tests/test_config.cpp`

- [ ] **Step 1: 写失败测试（`tests/test_config.cpp` 末尾追加）**

```cpp
TEST_CASE("config: rerank M5.2 keys default + override + key fallback") {
    // 默认值
    Config d = Config::from_map({});
    CHECK(d.rerank_base_url == "https://api.siliconflow.cn");
    CHECK(d.rerank_path == "/v1/rerank");
    CHECK(d.rerank_model == "Qwen/Qwen3-Reranker-8B");
    CHECK(d.rerank_timeout_sec == 20);
    CHECK_FALSE(d.rerank_instruction.empty());

    // rerank_key 为空时回退 deepseek_key
    Config f = Config::from_map({{"RAG_DEEPSEEK_KEY", "dsk-123"}});
    CHECK(f.rerank_key == "dsk-123");

    // 覆盖值 + rerank_key 独立时不回退
    Config o = Config::from_map({
        {"RAG_DEEPSEEK_KEY", "dsk-123"},
        {"RAG_RERANK_KEY", "rk-999"},
        {"RAG_RERANK_MODEL", "BAAI/bge-reranker-v2-m3"},
        {"RAG_RERANK_TIMEOUT_SEC", "45"},
        {"RAG_RERANK_INSTRUCTION", "自定义指令"},
    });
    CHECK(o.rerank_key == "rk-999");
    CHECK(o.rerank_model == "BAAI/bge-reranker-v2-m3");
    CHECK(o.rerank_timeout_sec == 45);
    CHECK(o.rerank_instruction == "自定义指令");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.\rag2.0.tests\x64\Debug\rag2.0.tests.exe -tc="config: rerank M5.2 keys default + override + key fallback"`
Expected: 编译失败（`rerank_base_url` 等成员不存在）。

- [ ] **Step 3: `src/rag_config.h` 加字段**

在 `int rerank_max_per_clause = 2;`（现 :31）之后加：

```cpp
    // M5.2 模型重排
    std::string rerank_base_url;
    std::string rerank_path;
    std::string rerank_model;
    std::string rerank_key;
    int         rerank_timeout_sec = 20;
    std::string rerank_instruction;
```

- [ ] **Step 4: `src/config.cpp` 解析（在 `c.rerank_max_per_clause = ...`（现 :35）之后）**

```cpp
    c.rerank_base_url   = get(e, "RAG_RERANK_BASE_URL", "https://api.siliconflow.cn");
    c.rerank_path       = get(e, "RAG_RERANK_PATH", "/v1/rerank");
    c.rerank_model      = get(e, "RAG_RERANK_MODEL", "Qwen/Qwen3-Reranker-8B");
    c.rerank_key        = get(e, "RAG_RERANK_KEY");
    if (c.rerank_key.empty()) c.rerank_key = c.deepseek_key;   // 空则复用 deepseek key
    c.rerank_timeout_sec = std::stoi(get(e, "RAG_RERANK_TIMEOUT_SEC", "20"));
    c.rerank_instruction = get(e, "RAG_RERANK_INSTRUCTION",
                               "检索与给定查询最相关的公路工程标准规范条款或试验方法片段");
```

- [ ] **Step 5: `config.example.json` 追加键**

在 `,"RAG_RERANK_MAX_PER_CLAUSE": "2"`（现 :23）之后：

```json
  ,"RAG_RERANK_MODEL": "Qwen/Qwen3-Reranker-8B"
  ,"RAG_RERANK_INSTRUCTION": "检索与给定查询最相关的公路工程标准规范条款或试验方法片段"
  ,"RAG_RERANK_KEY": ""
  ,"RAG_RERANK_TIMEOUT_SEC": "20"
```

- [ ] **Step 6: 构建 + 跑该测试确认通过**

Run: 先 MSBuild，再 `.\rag2.0.tests\x64\Debug\rag2.0.tests.exe -tc="config: rerank M5.2 keys default + override + key fallback"`
Expected: PASS。

- [ ] **Step 7: 提交**

```bash
git add src/rag_config.h src/config.cpp config.example.json tests/test_config.cpp
git commit -m "feat(rerank): M5.2 rerank API 配置项（含 key 回退 deepseek）" -- src/rag_config.h src/config.cpp config.example.json tests/test_config.cpp
```

---

## Task 2: RerankApiClient（请求体构造 + 响应解析 + score 网络调用）

**Files:**
- Create: `src/retrieve/rerank_api_client.h`, `src/retrieve/rerank_api_client.cpp`, `tests/test_rerank_api_client.cpp`
- Modify: `rag2.0/rag2.0.vcxproj`(+`.filters`), `rag2.0.tests/rag2.0.tests.vcxproj`(+`.filters`)

- [ ] **Step 1: 写 `tests/test_rerank_api_client.cpp`（失败测试）**

```cpp
#include <doctest/doctest.h>
#include "retrieve/rerank_api_client.h"
#include <nlohmann/json.hpp>

TEST_CASE("build_rerank_request_body: fields + omit instruction when empty") {
    auto body = build_rerank_request_body("M", "查询", "指令", {"d0", "d1"}, false);
    auto j = nlohmann::json::parse(body);
    CHECK(j["model"] == "M");
    CHECK(j["query"] == "查询");
    CHECK(j["instruction"] == "指令");
    CHECK(j["documents"].size() == 2);
    CHECK(j["documents"][0] == "d0");
    CHECK(j["return_documents"] == false);
    CHECK(j.contains("top_n") == false);            // 不发 top_n

    auto body2 = build_rerank_request_body("M", "q", "", {"d0"}, false);
    auto j2 = nlohmann::json::parse(body2);
    CHECK(j2.contains("instruction") == false);     // instruction 空则不写
}

TEST_CASE("parse_rerank_response: valid results -> index+score") {
    std::string r = R"({"id":"x","results":[
        {"index":2,"relevance_score":0.9},
        {"index":0,"relevance_score":0.5}]})";
    auto s = parse_rerank_response(r);
    REQUIRE(s.size() == 2);
    CHECK(s[0].index == 2);
    CHECK(s[0].score == doctest::Approx(0.9));
    CHECK(s[1].index == 0);
}

TEST_CASE("parse_rerank_response: invalid / empty rejected (throws)") {
    CHECK_THROWS(parse_rerank_response("not json"));
    CHECK_THROWS(parse_rerank_response(R"({"id":"x"})"));            // 无 results
    CHECK_THROWS(parse_rerank_response(R"({"results":[]})"));        // 空 results
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.\rag2.0.tests\x64\Debug\rag2.0.tests.exe -tc="build_rerank_request_body: fields + omit instruction when empty"`
Expected: 编译失败（头文件不存在）。

- [ ] **Step 3: 写 `src/retrieve/rerank_api_client.h`**

```cpp
#pragma once
#include <string>
#include <vector>

// 单个候选的模型打分。index = 请求 documents 数组下标。
struct RerankScore {
    int index = 0;
    double score = 0.0;
};

// 纯函数：构造 /v1/rerank 请求体。instruction 为空则不写该字段；不发 top_n。
std::string build_rerank_request_body(const std::string& model, const std::string& query,
                                      const std::string& instruction,
                                      const std::vector<std::string>& documents,
                                      bool return_documents);

// 纯函数：解析响应。要求 results 为非空数组、每项含 index 与 relevance_score。
// 结构不可用（非 JSON / 无 results / results 空）时抛 std::runtime_error。
std::vector<RerankScore> parse_rerank_response(const std::string& json_body);

// 网络客户端：复用 http::post_json。score() 失败（HTTP 非 2xx / 解析失败）抛异常，由调用方兜底。
class RerankApiClient {
public:
    RerankApiClient(std::string base_url, std::string path, std::string model,
                    std::string api_key, int timeout_sec, std::string instruction)
        : base_url_(std::move(base_url)), path_(std::move(path)), model_(std::move(model)),
          api_key_(std::move(api_key)), timeout_sec_(timeout_sec),
          instruction_(std::move(instruction)) {}
    std::vector<RerankScore> score(const std::string& query,
                                   const std::vector<std::string>& docs) const;
private:
    std::string base_url_, path_, model_, api_key_;
    int timeout_sec_;
    std::string instruction_;
};
```

- [ ] **Step 4: 写 `src/retrieve/rerank_api_client.cpp`**

```cpp
#include "retrieve/rerank_api_client.h"
#include "http/http_client.h"
#include <nlohmann/json.hpp>
#include <stdexcept>

using nlohmann::json;

std::string build_rerank_request_body(const std::string& model, const std::string& query,
                                      const std::string& instruction,
                                      const std::vector<std::string>& documents,
                                      bool return_documents) {
    json body;
    body["model"] = model;
    body["query"] = query;
    if (!instruction.empty()) body["instruction"] = instruction;
    body["documents"] = documents;
    body["return_documents"] = return_documents;
    // 有意不设 top_n：让模型给整池打分并全返回，漏排交本地补尾。
    return body.dump();
}

std::vector<RerankScore> parse_rerank_response(const std::string& json_body) {
    json j = json::parse(json_body, nullptr, /*allow_exceptions=*/false);
    if (!j.is_object() || !j.contains("results") || !j["results"].is_array() || j["results"].empty())
        throw std::runtime_error("rerank response invalid: missing/empty results");
    std::vector<RerankScore> out;
    for (const auto& r : j["results"]) {
        if (!r.contains("index") || !r.contains("relevance_score")) continue;
        RerankScore s;
        s.index = r["index"].get<int>();
        s.score = r["relevance_score"].get<double>();
        out.push_back(s);
    }
    if (out.empty()) throw std::runtime_error("rerank response invalid: no scored result");
    return out;
}

std::vector<RerankScore> RerankApiClient::score(const std::string& query,
                                                const std::vector<std::string>& docs) const {
    std::string body = build_rerank_request_body(model_, query, instruction_, docs, false);
    std::map<std::string, std::string> headers = {{"Authorization", "Bearer " + api_key_}};
    auto res = http::post_json(base_url_, path_, body, headers, timeout_sec_);
    if (!res.ok())
        throw std::runtime_error("rerank api failed: status=" + std::to_string(res.status) + " " + res.error);
    return parse_rerank_response(res.body);   // 解析失败抛，调用方兜底
}
```

- [ ] **Step 5: 注册 4 处工程文件**

`rag2.0/rag2.0.vcxproj`：`<ClCompile Include="..\src\retrieve\reranker.cpp" />`（:113）旁加 `<ClCompile Include="..\src\retrieve\rerank_api_client.cpp" />`；`<ClInclude Include="..\src\retrieve\reranker.h" />`（:167）旁加 `<ClInclude Include="..\src\retrieve\rerank_api_client.h" />`。
`rag2.0.tests/rag2.0.tests.vcxproj`：`<ClCompile Include="..\src\retrieve\reranker.cpp" />`（:150）旁加 `<ClCompile Include="..\src\retrieve\rerank_api_client.cpp" />`；`<ClCompile Include="..\tests\test_reranker.cpp" />`（:151）旁加 `<ClCompile Include="..\tests\test_rerank_api_client.cpp" />`。
两个 `.vcxproj.filters` 各自照抄 reranker 那两条（复制 `<Filter>` 归属），把文件名换成 rerank_api_client / test_rerank_api_client。

- [ ] **Step 6: 构建 + 跑三个 case 确认通过**

Run: MSBuild，再 `.\rag2.0.tests\x64\Debug\rag2.0.tests.exe -tc="*rerank_response*,build_rerank_request_body*"`
Expected: 全 PASS。

- [ ] **Step 7: 提交**

```bash
git add src/retrieve/rerank_api_client.h src/retrieve/rerank_api_client.cpp tests/test_rerank_api_client.cpp rag2.0/rag2.0.vcxproj rag2.0/rag2.0.vcxproj.filters rag2.0.tests/rag2.0.tests.vcxproj rag2.0.tests/rag2.0.tests.vcxproj.filters
git commit -m "feat(rerank): RerankApiClient 纯请求体/响应解析 + score 网络调用" -- src/retrieve/rerank_api_client.h src/retrieve/rerank_api_client.cpp tests/test_rerank_api_client.cpp rag2.0/rag2.0.vcxproj rag2.0/rag2.0.vcxproj.filters rag2.0.tests/rag2.0.tests.vcxproj rag2.0.tests/rag2.0.tests.vcxproj.filters
```

---

## Task 3: compose_rerank_document（紧凑文档构造）+ text_utf8::char_count

**Files:**
- Modify: `src/util/text_utf8.h`, `src/retrieve/reranker.h`, `src/retrieve/reranker.cpp`
- Test: `tests/test_reranker.cpp`

- [ ] **Step 1: 写失败测试（`tests/test_reranker.cpp` 末尾追加）**

```cpp
TEST_CASE("compose_rerank_document: 标题/编号/正文，忽略 path_text 与 bm25_text") {
    RerankCandidate c = mk("id1", "dense", 0, "5.1", "T0709", "马歇尔稳定度");
    c.atomic_text = "本方法规定了马歇尔稳定度试验的技术要求。";
    c.path_text = "不应出现的路径";
    c.bm25_text = "不应出现的BM25文本";
    std::string doc = compose_rerank_document(c);
    CHECK(doc.find("马歇尔稳定度") != std::string::npos);   // title
    CHECK(doc.find("条款 5.1") != std::string::npos);
    CHECK(doc.find("方法 T0709") != std::string::npos);
    CHECK(doc.find("技术要求") != std::string::npos);        // atomic
    CHECK(doc.find("不应出现的路径") == std::string::npos);   // path_text 不进
    CHECK(doc.find("不应出现的BM25文本") == std::string::npos);
}

TEST_CASE("compose_rerank_document: 空编号不写编号行；长正文短则补 context") {
    RerankCandidate c = mk("id2", "dense", 0, "", "", "");   // 无 title/clause/method
    c.atomic_text = "很短的正文";                            // < 80 字 -> 补 context
    c.context_text = "这是补充上下文内容。";
    std::string doc = compose_rerank_document(c);
    CHECK(doc.find("编号:") == std::string::npos);
    CHECK(doc.find("标题:") == std::string::npos);
    CHECK(doc.find("补充上下文") != std::string::npos);
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.\rag2.0.tests\x64\Debug\rag2.0.tests.exe -tc="compose_rerank_document*"`
Expected: 编译失败（`compose_rerank_document` 未声明）。

- [ ] **Step 3: `src/util/text_utf8.h` 加 `char_count`（在 `truncate` 之后、命名空间内）**

```cpp
// 可见字符数（一个汉字算 1）。纯函数。
inline size_t char_count(const std::string& s) {
    size_t i = 0, chars = 0;
    while (i < s.size()) {
        unsigned char b = static_cast<unsigned char>(s[i]);
        size_t step = (b < 0x80) ? 1 : ((b >> 5) == 0x6) ? 2 : ((b >> 4) == 0xE) ? 3 : 4;
        i += step;
        ++chars;
    }
    return chars;
}
```

- [ ] **Step 4: `src/retrieve/reranker.h` 声明（在 `assemble_rerank_candidates` 声明之后）**

```cpp
// document 构造版本号——改下面 compose 格式规则时必须 bump（缓存键含它）。
extern const char* kRerankDocVersion;

// 纯函数：把一个候选拼成给重排模型看的紧凑文档：标题 / 编号 / 正文。
// atomic 为主；title 只作定位；clause/method 非空才写；atomic 很短(<80 字)才补一小段 context；
// 整体按 UTF-8 字符上限截断。path_text、bm25_text、chunk id、score 一律不进。
std::string compose_rerank_document(const RerankCandidate& c);
```

- [ ] **Step 5: `src/retrieve/reranker.cpp` 实现（顶部加 `#include "util/text_utf8.h"`）**

```cpp
const char* kRerankDocVersion = "v1";

std::string compose_rerank_document(const RerankCandidate& c) {
    std::string out;
    if (!c.title.empty()) out += "标题: " + c.title + "\n";
    std::string num;
    if (!c.clause_no.empty()) num += "条款 " + c.clause_no;
    if (!c.method_no.empty()) { if (!num.empty()) num += "；"; num += "方法 " + c.method_no; }
    if (!num.empty()) out += "编号: " + num + "\n";
    std::string body = c.atomic_text;
    if (text_utf8::char_count(c.atomic_text) < 80 && !c.context_text.empty())
        body += "\n" + text_utf8::truncate(c.context_text, 200);   // 150-300 区间取 200
    out += "正文: " + body;
    return text_utf8::truncate(out, 1000);   // 单 document 上限（800-1200 区间取 1000）
}
```

- [ ] **Step 6: 构建 + 跑测试确认通过**

Run: MSBuild，再 `.\rag2.0.tests\x64\Debug\rag2.0.tests.exe -tc="compose_rerank_document*"`
Expected: 全 PASS。

- [ ] **Step 7: 提交**

```bash
git add src/util/text_utf8.h src/retrieve/reranker.h src/retrieve/reranker.cpp tests/test_reranker.cpp
git commit -m "feat(rerank): compose_rerank_document + text_utf8::char_count" -- src/util/text_utf8.h src/retrieve/reranker.h src/retrieve/reranker.cpp tests/test_reranker.cpp
```

---

## Task 4: 抽出共用后处理 finalize_rerank（+ light_rerank 改调它 + max_per_clause<=0 不掐）

**Files:**
- Modify: `src/retrieve/reranker.h`, `src/retrieve/reranker.cpp`
- Test: `tests/test_reranker.cpp`

- [ ] **Step 1: 写失败测试（追加）**

```cpp
TEST_CASE("finalize_rerank: clause dedup demotes 3rd same-clause, truncate") {
    std::vector<RerankCandidate> ranked = {
        mk("a","dense",0,"5.1"), mk("b","dense",1,"5.1"),
        mk("c","dense",2,"5.1"), mk("d","dense",3,"9.9")};
    auto out = finalize_rerank(ranked, 2, 10);
    REQUIRE(out.size() == 4);
    CHECK(out[0].chunk_id == "a");
    CHECK(out[1].chunk_id == "b");
    CHECK(out[2].chunk_id == "d");   // c 被降尾
    CHECK(out[3].chunk_id == "c");
}

TEST_CASE("finalize_rerank: max_per_clause<=0 means no cap") {
    std::vector<RerankCandidate> ranked = {
        mk("a","dense",0,"5.1"), mk("b","dense",1,"5.1"), mk("c","dense",2,"5.1")};
    auto out = finalize_rerank(ranked, 0, 10);
    REQUIRE(out.size() == 3);
    CHECK(out[0].chunk_id == "a");
    CHECK(out[1].chunk_id == "b");
    CHECK(out[2].chunk_id == "c");   // 不降尾
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.\rag2.0.tests\x64\Debug\rag2.0.tests.exe -tc="finalize_rerank*"`
Expected: 编译失败（`finalize_rerank` 未声明）。

- [ ] **Step 3: `src/retrieve/reranker.h` 声明（在 `light_rerank` 声明之前）**

```cpp
// 纯函数：所有 reranker 输出的共用后处理。按已排好序的 ranked 做同条款去重
// (standard_id|clause_no 超过 max_per_clause 的降到队尾；max_per_clause<=0 表示不掐；
// clause_no 为空不受限)，再截断 top_k。返回 base 候选。
std::vector<Candidate> finalize_rerank(const std::vector<RerankCandidate>& ranked,
                                       int max_per_clause, int top_k);
```

- [ ] **Step 4: `src/retrieve/reranker.cpp` 实现 + 改造 light_rerank**

新增（放在 `light_rerank` 之前）：

```cpp
std::vector<Candidate> finalize_rerank(const std::vector<RerankCandidate>& ranked,
                                       int max_per_clause, int top_k) {
    std::vector<Candidate> kept, overflow;
    std::map<std::string, int> clause_count;
    for (const auto& c : ranked) {
        if (max_per_clause > 0 && !c.clause_no.empty()) {
            std::string key = c.base.standard_id + "|" + c.clause_no;
            if (++clause_count[key] > max_per_clause) { overflow.push_back(c.base); continue; }
        }
        kept.push_back(c.base);
    }
    kept.insert(kept.end(), overflow.begin(), overflow.end());
    if (static_cast<int>(kept.size()) > top_k) kept.resize(top_k);
    return kept;
}
```

把现有 `light_rerank` 的**尾部去重/截断段（现 :52-64，从 `std::vector<Candidate> kept, overflow;` 到 `return kept;`）整段替换为**：

```cpp
    std::vector<RerankCandidate> ranked;
    ranked.reserve(sc.size());
    for (const auto& s : sc) ranked.push_back(pool[s.idx]);
    return finalize_rerank(ranked, max_per_clause, top_k);
```

（`light_rerank` 上半段打分 + `std::stable_sort` 不动。）

- [ ] **Step 5: 构建 + 跑 reranker 全组，确认新测试 + 旧 light 用例都绿**

Run: MSBuild，再 `.\rag2.0.tests\x64\Debug\rag2.0.tests.exe -tc="finalize_rerank*,light_rerank*"`
Expected: 全 PASS（旧 `clause dedup`/`stable+truncate` 是回归护栏）。

- [ ] **Step 6: 提交**

```bash
git add src/retrieve/reranker.h src/retrieve/reranker.cpp tests/test_reranker.cpp
git commit -m "refactor(rerank): 抽出共用 finalize_rerank（含 max_per_clause<=0 不掐），light 改调它" -- src/retrieve/reranker.h src/retrieve/reranker.cpp tests/test_reranker.cpp
```

---

## Task 5: assemble_model_ranking（模型输出排序规则，纯函数）

**Files:**
- Modify: `src/retrieve/reranker.h`（加 `#include "retrieve/rerank_api_client.h"`）, `src/retrieve/reranker.cpp`
- Test: `tests/test_reranker.cpp`

- [ ] **Step 1: 写失败测试（追加）**

```cpp
TEST_CASE("assemble_model_ranking: score desc, dup index first-only, OOB ignore, missing tail") {
    std::vector<RerankCandidate> pool = {
        mk("a","dense",0), mk("b","dense",1), mk("c","dense",2)};
    std::vector<RerankScore> scores = {
        {1, 0.9}, {0, 0.8}, {1, 0.1}, {5, 0.99}};   // b>a；重复 index1 忽略第二次；index5 越界忽略；c 缺分
    auto out = assemble_model_ranking(pool, scores, 2 /*cap*/, 10 /*top_k*/);
    REQUIRE(out.size() == 3);
    CHECK(out[0].chunk_id == "b");
    CHECK(out[1].chunk_id == "a");
    CHECK(out[2].chunk_id == "c");   // 未打分 -> RRF 原序补尾
}

TEST_CASE("assemble_model_ranking: dedup + truncate applied") {
    std::vector<RerankCandidate> pool = {
        mk("a","dense",0,"5.1"), mk("b","dense",1,"5.1"),
        mk("c","dense",2,"5.1"), mk("d","dense",3,"9.9")};
    std::vector<RerankScore> scores = {{0,0.9},{1,0.8},{2,0.7},{3,0.6}};
    auto out = assemble_model_ranking(pool, scores, 2, 10);
    REQUIRE(out.size() == 4);
    CHECK(out[2].chunk_id == "d");   // c(第3个5.1)降尾
    CHECK(out[3].chunk_id == "c");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.\rag2.0.tests\x64\Debug\rag2.0.tests.exe -tc="assemble_model_ranking*"`
Expected: 编译失败。

- [ ] **Step 3: `src/retrieve/reranker.h` — 顶部加 include，声明函数**

顶部 `#include` 区加：`#include "retrieve/rerank_api_client.h"`（为 `RerankScore`）。

在 `finalize_rerank` 声明之后加：

```cpp
// 纯函数：把模型打分套到 pool 上，生成最终排序。有效候选按 score 降序(稳定)；
// 重复 index 只取第一次；越界 index 忽略；未打分候选按 pool(RRF) 原序补尾；
// 再走 finalize_rerank(去重+截断)。
std::vector<Candidate> assemble_model_ranking(const std::vector<RerankCandidate>& pool,
                                              const std::vector<RerankScore>& scores,
                                              int max_per_clause, int top_k);
```

- [ ] **Step 4: `src/retrieve/reranker.cpp` 实现**

```cpp
std::vector<Candidate> assemble_model_ranking(const std::vector<RerankCandidate>& pool,
                                              const std::vector<RerankScore>& scores,
                                              int max_per_clause, int top_k) {
    const int n = static_cast<int>(pool.size());
    std::vector<char> seen(n, 0);
    struct SI { size_t idx; double score; };
    std::vector<SI> scored;
    for (const auto& rs : scores) {
        if (rs.index < 0 || rs.index >= n) continue;   // 越界忽略
        if (seen[rs.index]) continue;                   // 重复 index 取第一次
        seen[rs.index] = 1;
        scored.push_back({static_cast<size_t>(rs.index), rs.score});
    }
    std::stable_sort(scored.begin(), scored.end(),
                     [](const SI& a, const SI& b) { return a.score > b.score; });
    std::vector<RerankCandidate> ranked;
    ranked.reserve(n);
    for (const auto& s : scored) ranked.push_back(pool[s.idx]);
    for (int i = 0; i < n; ++i) if (!seen[i]) ranked.push_back(pool[i]);   // 补尾
    return finalize_rerank(ranked, max_per_clause, top_k);
}
```

- [ ] **Step 5: 构建 + 跑测试确认通过**

Run: MSBuild，再 `.\rag2.0.tests\x64\Debug\rag2.0.tests.exe -tc="assemble_model_ranking*"`
Expected: 全 PASS。

- [ ] **Step 6: 提交**

```bash
git add src/retrieve/reranker.h src/retrieve/reranker.cpp tests/test_reranker.cpp
git commit -m "feat(rerank): assemble_model_ranking 模型输出排序规则（纯函数）" -- src/retrieve/reranker.h src/retrieve/reranker.cpp tests/test_reranker.cpp
```

---

## Task 6: ModelReranker + RrfPassthrough + 打分缓存

**Files:**
- Modify: `src/retrieve/reranker.h`, `src/retrieve/reranker.cpp`
- Test: `tests/test_reranker.cpp`

- [ ] **Step 1: 写失败测试（追加）**

用注入的假 `score_call` + 临时缓存目录，覆盖成功/失败回退/缓存命中三条路径。

```cpp
#include <filesystem>   // 若文件顶部尚无

// 计数并可控抛异常的假打分器
struct FakeScorer {
    int calls = 0;
    bool throw_next = false;
    std::vector<RerankScore> ret;
    std::vector<RerankScore> operator()(const std::string&, const std::vector<std::string>&) {
        ++calls;
        if (throw_next) throw std::runtime_error("boom");
        return ret;
    }
};

static std::string tmp_cache_dir(const std::string& name) {
    auto d = std::filesystem::temp_directory_path() / ("rr_" + name);
    std::filesystem::remove_all(d);
    return d.string();
}

TEST_CASE("ModelReranker: success uses model order, writes+reads cache") {
    QueryAnalysis qa; qa.clean_text = "查询X";
    std::vector<RerankCandidate> pool = {mk("a","dense",0), mk("b","dense",1)};
    auto scorer = std::make_shared<FakeScorer>();
    scorer->ret = {{1, 0.9}, {0, 0.1}};   // b 优先
    std::string dir = tmp_cache_dir("ok");
    RrfPassthrough fb;
    RerankCall call = [scorer](const std::string& q, const std::vector<std::string>& d){ return (*scorer)(q,d); };
    ModelReranker mr(call, "M", "指令", dir, 2, &fb);

    auto out1 = mr.rerank(qa, pool, 10);
    REQUIRE(out1.size() == 2);
    CHECK(out1[0].chunk_id == "b");
    CHECK(scorer->calls == 1);

    // 第二次同输入：命中缓存，不再调用打分器
    auto out2 = mr.rerank(qa, pool, 10);
    CHECK(out2[0].chunk_id == "b");
    CHECK(scorer->calls == 1);           // 未增加 -> 缓存命中
    std::filesystem::remove_all(dir);
}

TEST_CASE("ModelReranker: model failure falls back to injected reranker (RRF passthrough)") {
    QueryAnalysis qa; qa.clean_text = "查询Y";
    std::vector<RerankCandidate> pool = {mk("a","dense",0), mk("b","dense",1)};
    auto scorer = std::make_shared<FakeScorer>();
    scorer->throw_next = true;
    std::string dir = tmp_cache_dir("fail");
    RrfPassthrough fb;
    RerankCall call = [scorer](const std::string& q, const std::vector<std::string>& d){ return (*scorer)(q,d); };
    ModelReranker mr(call, "M", "", dir, 2, &fb);

    auto out = mr.rerank(qa, pool, 10);
    REQUIRE(out.size() == 2);
    CHECK(out[0].chunk_id == "a");       // RRF 原序
    CHECK(out[1].chunk_id == "b");
    std::filesystem::remove_all(dir);
}

TEST_CASE("RrfPassthrough: returns pool base in order, truncated, no dedup") {
    QueryAnalysis qa;
    std::vector<RerankCandidate> pool = {
        mk("a","dense",0,"5.1"), mk("b","dense",1,"5.1"), mk("c","dense",2,"5.1")};
    RrfPassthrough fb;
    auto out = fb.rerank(qa, pool, 2);
    REQUIRE(out.size() == 2);
    CHECK(out[0].chunk_id == "a");
    CHECK(out[1].chunk_id == "b");       // 不去重、不降尾，仅截断
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.\rag2.0.tests\x64\Debug\rag2.0.tests.exe -tc="ModelReranker*,RrfPassthrough*"`
Expected: 编译失败。

- [ ] **Step 3: `src/retrieve/reranker.h` 声明（在 `LightReranker` 类之后）**

```cpp
// 模型打分回调：入参 query + documents(RRF序)，返回 index+score。失败抛异常或返回空。
using RerankCall = std::function<std::vector<RerankScore>(const std::string& query,
                                                          const std::vector<std::string>& docs)>;

// 兜底：直接返回 pool 的 base（RRF 原序）截断到 top_k，不去重。model 失败回退用。
class RrfPassthrough : public IReranker {
public:
    std::vector<Candidate> rerank(const QueryAnalysis& qa,
                                  const std::vector<RerankCandidate>& pool, int top_k) override;
};

// 模型重排：组文档 → 查缓存 → 调 score_call → 成功 assemble_model_ranking、失败走注入的兜底。
class ModelReranker : public IReranker {
public:
    ModelReranker(RerankCall call, std::string model, std::string instruction,
                  std::string cache_dir, int max_per_clause, IReranker* fallback)
        : call_(std::move(call)), model_(std::move(model)), instruction_(std::move(instruction)),
          cache_dir_(std::move(cache_dir)), max_per_clause_(max_per_clause), fallback_(fallback) {}
    std::vector<Candidate> rerank(const QueryAnalysis& qa,
                                  const std::vector<RerankCandidate>& pool, int top_k) override;
private:
    RerankCall call_;
    std::string model_, instruction_, cache_dir_;
    int max_per_clause_;
    IReranker* fallback_;
};
```

`reranker.h` 顶部 `#include` 区补 `#include <functional>`（若无）。

- [ ] **Step 4: `src/retrieve/reranker.cpp` 实现**

顶部补 include：`#include "query/query_planner.h"`（`normalize_question`）、`#include <nlohmann/json.hpp>`、`#include <filesystem>`、`#include <fstream>`、`#include <sstream>`、`#include <cstdio>`、`#include <functional>`。

```cpp
namespace {
std::string rerank_query_text(const QueryAnalysis& qa) {
    return qa.dense_text.empty() ? qa.clean_text : qa.dense_text;
}
std::string rerank_cache_path(const std::string& dir, const std::string& model,
                              const std::string& instruction, const std::string& query,
                              const std::vector<RerankCandidate>& pool) {
    std::vector<std::string> ids;
    ids.reserve(pool.size());
    for (const auto& c : pool) ids.push_back(c.base.chunk_id);
    std::sort(ids.begin(), ids.end());
    std::string joined;
    for (const auto& id : ids) { joined += id; joined += ','; }
    std::string key = model + "\x1f" + instruction + "\x1f" + normalize_question(query) +
                      "\x1f" + joined + "\x1f" + kRerankDocVersion;
    size_t h = std::hash<std::string>{}(key);
    char name[32];
    std::snprintf(name, sizeof(name), "%016llx.json", static_cast<unsigned long long>(h));
    return dir + "/" + name;
}
// 读缓存 -> chunk_id->score；无/坏文件返回空 map。
std::map<std::string, double> read_rerank_cache(const std::string& path) {
    std::ifstream f(path, std::ios::binary);
    if (!f) return {};
    std::stringstream ss; ss << f.rdbuf();
    auto j = nlohmann::json::parse(ss.str(), nullptr, false);
    std::map<std::string, double> m;
    if (j.is_object() && j.contains("scores") && j["scores"].is_object())
        for (auto it = j["scores"].begin(); it != j["scores"].end(); ++it)
            m[it.key()] = it.value().get<double>();
    return m;
}
void write_rerank_cache(const std::string& dir, const std::string& path,
                        const std::map<std::string, double>& scores) {
    std::error_code ec;
    std::filesystem::create_directories(dir, ec);
    nlohmann::json j; j["scores"] = nlohmann::json::object();
    for (const auto& kv : scores) j["scores"][kv.first] = kv.second;
    std::ofstream f(path, std::ios::binary);
    if (f) f << j.dump();   // 写失败非致命
}
// chunk_id->score map 映射回 index-based scores。
std::vector<RerankScore> scores_from_map(const std::vector<RerankCandidate>& pool,
                                         const std::map<std::string, double>& m) {
    std::vector<RerankScore> out;
    for (int i = 0; i < static_cast<int>(pool.size()); ++i) {
        auto it = m.find(pool[i].base.chunk_id);
        if (it != m.end()) out.push_back({i, it->second});
    }
    return out;
}
}  // namespace

std::vector<Candidate> RrfPassthrough::rerank(const QueryAnalysis&,
                                              const std::vector<RerankCandidate>& pool, int top_k) {
    std::vector<Candidate> out;
    out.reserve(pool.size());
    for (const auto& c : pool) out.push_back(c.base);
    if (static_cast<int>(out.size()) > top_k) out.resize(top_k);
    return out;
}

std::vector<Candidate> ModelReranker::rerank(const QueryAnalysis& qa,
                                             const std::vector<RerankCandidate>& pool, int top_k) {
    const std::string query = rerank_query_text(qa);
    const std::string path = rerank_cache_path(cache_dir_, model_, instruction_, query, pool);

    // 1) 缓存命中：直接用缓存分。
    auto cached = read_rerank_cache(path);
    if (!cached.empty()) {
        auto scores = scores_from_map(pool, cached);
        if (!scores.empty())
            return assemble_model_ranking(pool, scores, max_per_clause_, top_k);
    }

    // 2) 调模型；任何失败/空 -> 走兜底。
    std::vector<std::string> docs;
    docs.reserve(pool.size());
    for (const auto& c : pool) docs.push_back(compose_rerank_document(c));
    std::vector<RerankScore> scores;
    try {
        scores = call_(query, docs);
    } catch (const std::exception& e) {
        spdlog::warn("[rerank] 模型调用失败，回退兜底: {}", e.what());
        return fallback_->rerank(qa, pool, top_k);
    }
    if (scores.empty()) {
        spdlog::warn("[rerank] 模型返回空分，回退兜底");
        return fallback_->rerank(qa, pool, top_k);
    }

    // 3) 成功：写缓存（chunk_id->score），再 assemble。
    std::map<std::string, double> to_cache;
    const int n = static_cast<int>(pool.size());
    for (const auto& s : scores)
        if (s.index >= 0 && s.index < n) to_cache[pool[s.index].base.chunk_id] = s.score;
    write_rerank_cache(cache_dir_, path, to_cache);
    return assemble_model_ranking(pool, scores, max_per_clause_, top_k);
}
```

`reranker.cpp` 若还没 `#include <spdlog/spdlog.h>` 与 `#include <algorithm>`，一并补上。

- [ ] **Step 5: 构建 + 跑测试确认通过**

Run: MSBuild，再 `.\rag2.0.tests\x64\Debug\rag2.0.tests.exe -tc="ModelReranker*,RrfPassthrough*"`
Expected: 全 PASS（含缓存命中 `calls==1`、失败回退 RRF）。

- [ ] **Step 6: 提交**

```bash
git add src/retrieve/reranker.h src/retrieve/reranker.cpp tests/test_reranker.cpp
git commit -m "feat(rerank): ModelReranker + RrfPassthrough + 打分磁盘缓存（chunk_id->score）" -- src/retrieve/reranker.h src/retrieve/reranker.cpp tests/test_reranker.cpp
```

---

## Task 7: RerankParams 扩容 + text_retrieve 多态接线 + make_rerank 注入

**Files:**
- Modify: `src/retrieve/reranker.h`（RerankParams）, `src/retrieve/text_search.cpp`, `src/main.cpp`

- [ ] **Step 1: `src/retrieve/reranker.h` — RerankParams 加字段**

在 `struct RerankParams { ... int max_per_clause = 2;` 之后加：

```cpp
    // M5.2：model/hybrid 用
    std::string model;
    std::string instruction;
    std::string cache_dir = "data/rerank_cache";
    RerankCall  score_call;   // off/light 时留空
```

（`RerankCall` 已在 Task 6 定义于本头文件；注意把 `RerankParams` 的定义**移到 `RerankCall`/`RerankScore` 可见之后**，即放在 `using RerankCall = ...;` 之下。若原 `RerankParams` 在文件顶部，整块下移到 `ModelReranker` 声明区附近。）

- [ ] **Step 2: `src/main.cpp` — make_rerank 注入 client 闭包**

顶部加 `#include "retrieve/rerank_api_client.h"`。把 `make_rerank`（现 :73）改为：

```cpp
static RerankParams make_rerank(const Config& cfg) {
    RerankParams r;
    r.mode = cfg.rerank_mode.empty() ? "off" : cfg.rerank_mode;
    r.pool_mult = cfg.rerank_pool_mult;
    r.max_per_clause = cfg.rerank_max_per_clause;
    r.model = cfg.rerank_model;
    r.instruction = cfg.rerank_instruction;
    r.cache_dir = "data/rerank_cache";
    if (r.mode == "model" || r.mode == "hybrid") {
        RerankApiClient client(cfg.rerank_base_url, cfg.rerank_path, cfg.rerank_model,
                               cfg.rerank_key, cfg.rerank_timeout_sec, cfg.rerank_instruction);
        r.score_call = [client](const std::string& q, const std::vector<std::string>& docs) {
            return client.score(q, docs);   // client 按值捕获，自足、无生命周期问题
        };
    }
    return r;
}
```

- [ ] **Step 3: `src/retrieve/text_search.cpp` — do_rerank 分支改多态**

把 `const bool do_rerank = (rerank.mode == "light") && !list_recall;`（现 :71）改为：

```cpp
    const bool do_rerank =
        (rerank.mode == "light" || rerank.mode == "model" || rerank.mode == "hybrid") && !list_recall;
```

把 `else if (do_rerank) { ... light_rerank_with_fallback ... }` 段（现 :77-82）替换为：

```cpp
    } else if (do_rerank) {
        std::vector<RerankCandidate> pool = build_rerank_candidates(fused, pg);
        if (pool.empty()) {
            if (!fused.empty()) spdlog::warn("[rerank] PG 回查候选全失败，回退 RRF 原顺序");
            if (static_cast<int>(fused.size()) > top_k) fused.resize(top_k);
        } else if (rerank.mode == "light") {
            LightReranker light(rerank.max_per_clause);
            fused = light.rerank(qa, pool, top_k);
        } else {   // model | hybrid
            LightReranker light(rerank.max_per_clause);
            RrfPassthrough rrf_pass;
            IReranker* fb = (rerank.mode == "hybrid")
                          ? static_cast<IReranker*>(&light) : static_cast<IReranker*>(&rrf_pass);
            ModelReranker model(rerank.score_call, rerank.model, rerank.instruction,
                                rerank.cache_dir, rerank.max_per_clause, fb);
            fused = model.rerank(qa, pool, top_k);
        }
    }
```

（`light_rerank_with_fallback` 保留不删——其 doctest 仍在；只是 text_retrieve 不再走它。）

- [ ] **Step 4: 构建 + 跑全量单测**

Run: MSBuild，再 `.\rag2.0.tests\x64\Debug\rag2.0.tests.exe`
Expected: 全绿（doctest 全量；确认现有 213+ 用例 + 本 plan 新增全过）。

- [ ] **Step 5: 冒烟——off/light 行为不变**

Run（PowerShell，需本地 PG+Milvus 起着，见 [[local-services]]）：
```powershell
$env:RAG_RERANK_MODE="off";   .\rag2.0\x64\Debug\rag2.0.exe retrievecheck "沥青混合料马歇尔稳定度技术要求"
$env:RAG_RERANK_MODE="light"; .\rag2.0\x64\Debug\rag2.0.exe retrievecheck "沥青混合料马歇尔稳定度技术要求"
```
Expected: 两条都正常返回候选、不崩（off 与既有一致）。若无本地服务，跳过此步、记为待人工跑。

- [ ] **Step 6: 提交**

```bash
git add src/retrieve/reranker.h src/retrieve/text_search.cpp src/main.cpp
git commit -m "feat(rerank): text_retrieve 走 IReranker 多态 + make_rerank 注入 RerankApiClient" -- src/retrieve/reranker.h src/retrieve/text_search.cpp src/main.cpp
```

---

## Task 8: 四模式 rich eval 对比 + README baseline + 收尾

**Files:**
- Modify: `README.md`
- Run: 全量 doctest + rich eval（需本地 PG+Milvus + SiliconFlow/DeepSeek 余额）

- [ ] **Step 1: 全量 doctest 复核**

Run: `.\rag2.0.tests\x64\Debug\rag2.0.tests.exe`
Expected: 全绿。记录总用例数。

- [ ] **Step 2: 跑四模式 rich eval（每次切 `RAG_RERANK_MODE` 后重跑同一命令）**

```powershell
# 需先起 PG + Milvus（见 [[local-services]]）；确认 SiliconFlow rerank key 可用
foreach ($m in "off","light","model","hybrid") {
  $env:RAG_RERANK_MODE=$m
  Write-Host "=== MODE=$m ==="
  .\rag2.0\x64\Debug\rag2.0.exe eval eval/retrieval_questions_100.annotated.json 30 rule --rich
}
```
把每模式的 **Group Recall@20 / Complete@20 / nDCG@20 / Redundancy@10 / Distractor Hit@10 / Distractor-before-gold** 填进下表：

| mode | Group Recall@20 | Complete@20 | nDCG@20 | Redundancy@10 | Distractor Hit@10 | Distractor-before-gold |
|------|---|---|---|---|---|---|
| off | | | | | | |
| light | | | | | | |
| model | | | | | | |
| hybrid | | | | | | |

- [ ] **Step 3: 定默认模式（依数据，不猜）**

判据（spec §评估）：仅当 `model` 或 `hybrid` **明显降低排序风险（Distractor-before-gold / Redundancy 下降）且不伤 Group Recall/Complete** 时，才把默认从 `off` 改成它；否则默认保持 `off`（代码默认已是 off，无需改）。若要改默认，改 `config.example.json` 的 `RAG_RERANK_MODE`。

- [ ] **Step 4: 更新 `README.md`**

在 M5.1 那行（README:126 附近）之后补一行 M5.2：写清 `RAG_RERANK_MODE=model|hybrid` 已落地、Qwen3-Reranker-8B、打分缓存、四模式 baseline 数字（用 Step 2 实测填），以及最终默认值与理由。禁止编造数字——没跑就标"待实测"。

- [ ] **Step 5: 提交**

```bash
git add README.md docs/superpowers/plans/2026-07-01-m5.2-model-reranker.md
git commit -m "docs(rerank): M5.2 四模式 baseline + README + 默认值结论" -- README.md docs/superpowers/plans/2026-07-01-m5.2-model-reranker.md
```

（若 Step 3 改了 `config.example.json` 默认，一并 `git add`/pathspec 提交。）

---

## Self-Review（写完后自查，已过）

- **Spec 覆盖**：API client 边界+契约→T2；配置（含 instruction/key 回退）→T1；输入构造（compose+长度闸+instruction 独立字段）→T3/T2；输出处理（relevance_score/index/排序规则）→T2/T5；缓存（键+chunk_id 存分+缓存外去重+失败不写）→T6；装配与多态（IReranker/model=RRF兜底/hybrid=Light兜底）→T6/T7；多样性后处理（finalize_rerank 抽出+统一 max_per_clause+model失败不走）→T4/T5/T7；模式语义→T7；错误处理（各 fallback+不写缓存+不打印 key）→T2/T6；测试清单→逐 Task 的 doctest；评估→T8。
- **占位符**：无 TBD/TODO；每个 code step 含完整代码。T8 的 baseline 表是"实测填入"，非占位（数据依赖，禁编造）。
- **类型一致**：`RerankScore{index:int,score:double}`（T2）贯穿 T5/T6；`RerankCall`（T6）用于 T7 RerankParams 与 make_rerank；`finalize_rerank`（T4）被 `light_rerank`（T4）与 `assemble_model_ranking`（T5）复用；`ModelReranker`/`RrfPassthrough` 签名（T6）与 text_retrieve 用法（T7）一致；`kRerankDocVersion`（T3）用于缓存键（T6）。
- **已知外部依赖**：T7 Step5、T8 需本地 PG+Milvus + rerank API 余额；无服务时按步内说明跳过并标记人工补跑。
