# M4.1 列举题召回补全与错片下压 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 对"列举 + 命中关键词（如天平）"的查询，新增 PG 关键词直查路把所有含关键词的片段补全召回，并在截断前的大池上把不含关键词的片段下压重排，把 `coverage` 从 52% 抬向接近满分。

**Architecture:** 两个小单元 + 一处接线：(1) 纯函数 `demote_without_keyterms`（rrf 里，稳定划分+截断，可单测）；(2) `PgClient::chunks_containing`（`embedding_text LIKE` + 现行过滤，仿 `chunks_by_method`）；(3) `text_retrieve` 在 `ListByCondition`+`key_terms` 时调直查路并入融合大池、再下压截断。只动列举意图，其余零回归。

**Tech Stack:** C++20、MSBuild + vcpkg、libpqxx、nlohmann/json、spdlog、doctest。沿用 [[rag2-build-setup]]。

**Scope：** 见 spec [`2026-06-17-m4.1-recall-completion-demotion-design.md`](../specs/2026-06-17-m4.1-recall-completion-demotion-design.md)。**不**接 rerank 模型/学习型稀疏，**不**删除候选，**不**自动放大列举题 k，**不**改 Milvus schema。分支 V3.2。

**无需改工程文件**：本刀只改既有文件（`rrf.{h,cpp}`、`pg_client.{h,cpp}`、`text_search.cpp`、`test_rrf.cpp`），都已登记在两个 vcxproj，无新增文件。

**构建/测试（PowerShell，项目根目录）：**
```powershell
& "D:\vs2022\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" rag2.0.sln /p:Configuration=Debug /p:Platform=x64 /m
.\rag2.0.tests\x64\Debug\rag2.0.tests.exe
```
应用 exe：`rag2.0\x64\Debug\rag2.0.exe`。

---

## Task 1: `demote_without_keyterms` 纯函数（TDD）

**Files:**
- Modify: `src/retrieve/rrf.h`
- Modify: `src/retrieve/rrf.cpp`
- Test: `tests/test_rrf.cpp`

- [ ] **Step 1: 在 `tests/test_rrf.cpp` 末尾追加失败测试**

```cpp
TEST_CASE("demote_without_keyterms keeps key-term hits in front, others after") {
    std::vector<Candidate> fused = {
        cand("S","S:no1#main",0.9f,"dense+bm25"),   // 不含关键词，但分高
        cand("S","S:yes1#main",0.5f,"keyterm"),     // 含关键词
        cand("S","S:no2#main",0.4f,"bm25"),
        cand("S","S:yes2#main",0.3f,"dense+keyterm") };
    auto out = demote_without_keyterms(fused, {"S:yes1#main","S:yes2#main"}, 10);
    REQUIRE(out.size() == 4);
    CHECK(out[0].chunk_id == "S:yes1#main");   // 命中的提前，组内保持原顺序
    CHECK(out[1].chunk_id == "S:yes2#main");
    CHECK(out[2].chunk_id == "S:no1#main");    // 未命中的压后，组内保持原顺序
    CHECK(out[3].chunk_id == "S:no2#main");
}

TEST_CASE("demote_without_keyterms truncates to top_k after reordering") {
    std::vector<Candidate> fused = {
        cand("S","S:no1#main",0.9f,"dense"),
        cand("S","S:yes1#main",0.5f,"keyterm"),
        cand("S","S:no2#main",0.4f,"dense") };
    auto out = demote_without_keyterms(fused, {"S:yes1#main"}, 2);
    REQUIRE(out.size() == 2);
    CHECK(out[0].chunk_id == "S:yes1#main");   // 命中的先进 top-2
    CHECK(out[1].chunk_id == "S:no1#main");
}

TEST_CASE("demote_without_keyterms with empty hit set preserves order") {
    std::vector<Candidate> fused = {
        cand("S","S:a#main",0.9f,"dense"), cand("S","S:b#main",0.5f,"bm25") };
    auto out = demote_without_keyterms(fused, {}, 10);
    REQUIRE(out.size() == 2);
    CHECK(out[0].chunk_id == "S:a#main");
    CHECK(out[1].chunk_id == "S:b#main");
}
```

（`cand(...)` 辅助函数已在 `test_rrf.cpp` 顶部定义，直接复用。）

- [ ] **Step 2: 在 `src/retrieve/rrf.h` 声明（紧跟 `pin_exact_clause` 声明之后）**

```cpp
// 错片下压：chunk_id 在 key_hit_ids 中的候选稳定排到前面（组内保持 fused 原序），
// 其余排后，最后截断 top_k。不删除、纯函数。用于列举题"含关键词的提前"。
std::vector<Candidate> demote_without_keyterms(const std::vector<Candidate>& fused,
                                               const std::vector<std::string>& key_hit_ids,
                                               int top_k);
```

- [ ] **Step 3: 在 `src/retrieve/rrf.cpp` 末尾实现**

`rrf.cpp` 顶部已 `#include <set>`（`pin_exact_clause` 用到）。追加：

```cpp
std::vector<Candidate> demote_without_keyterms(const std::vector<Candidate>& fused,
                                               const std::vector<std::string>& key_hit_ids,
                                               int top_k) {
    std::set<std::string> hits(key_hit_ids.begin(), key_hit_ids.end());
    std::vector<Candidate> front, back;
    front.reserve(fused.size());
    for (const auto& c : fused) {
        if (hits.count(c.chunk_id)) front.push_back(c);
        else back.push_back(c);
    }
    std::vector<Candidate> out;
    out.reserve(fused.size());
    out.insert(out.end(), front.begin(), front.end());
    out.insert(out.end(), back.begin(), back.end());
    if (static_cast<int>(out.size()) > top_k) out.resize(top_k);
    return out;
}
```

- [ ] **Step 4: 构建并运行 rrf 测试**

```powershell
& "D:\vs2022\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" rag2.0.sln /p:Configuration=Debug /p:Platform=x64 /m
.\rag2.0.tests\x64\Debug\rag2.0.tests.exe --test-case="*demote_without_keyterms*"
.\rag2.0.tests\x64\Debug\rag2.0.tests.exe
```
Expected：构建通过；3 个 demote 用例 PASS；全量绿（应为 161 用例，0 failed）。

- [ ] **Step 5: Commit**

```powershell
git add src/retrieve/rrf.h src/retrieve/rrf.cpp tests/test_rrf.cpp
git commit -m @'
feat(retrieve): demote_without_keyterms (stable demote of non-keyterm candidates)

含关键词的候选稳定提前、其余压后再截断 top_k，纯函数，供列举题错片下压。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
'@
```

---

## Task 2: `PgClient::chunks_containing`（关键词直查路）

**Files:**
- Modify: `src/db/pg_client.h`
- Modify: `src/db/pg_client.cpp`

无单测（依赖真库，沿用项目惯例；真验收在 Task 3 的 `rag2 eval`）。

- [ ] **Step 1: 在 `src/db/pg_client.h` 的 `PgClient` 里声明（紧跟 `chunks_by_method` 声明之后）**

```cpp
    // 关键词内容直查：embedding_text 含 keyword 的全部片段（status 非空则按其过滤现行）。
    // 只填 chunk_id/standard_id，按 chunk_id 排序。用于列举题补全召回。
    std::vector<RetrievalChunkRow> chunks_containing(const std::string& keyword,
                                                     const std::string& status);
```

- [ ] **Step 2: 在 `src/db/pg_client.cpp` 末尾实现（仿 `chunks_by_method`）**

```cpp
std::vector<RetrievalChunkRow> PgClient::chunks_containing(const std::string& keyword,
                                                           const std::string& status) {
    pqxx::connection cn(conninfo_);
    pqxx::work tx(cn);
    pqxx::result r;
    if (status.empty()) {
        r = tx.exec(
            "SELECT chunk_id,standard_id FROM retrieval_chunks "
            "WHERE embedding_text LIKE $1 ORDER BY chunk_id",
            pqxx::params{"%" + keyword + "%"});
    } else {
        r = tx.exec(
            "SELECT rc.chunk_id,rc.standard_id FROM retrieval_chunks rc "
            "JOIN standards s ON rc.standard_id=s.standard_id "
            "WHERE rc.embedding_text LIKE $1 AND s.status=$2 ORDER BY rc.chunk_id",
            pqxx::params{"%" + keyword + "%", status});
    }
    std::vector<RetrievalChunkRow> out;
    for (auto row : r) {
        RetrievalChunkRow c;
        c.chunk_id = row[0].c_str();
        c.standard_id = row[1].c_str();
        out.push_back(std::move(c));
    }
    return out;
}
```

- [ ] **Step 3: 构建 + 全量测试（确认不破坏现有）**

```powershell
& "D:\vs2022\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" rag2.0.sln /p:Configuration=Debug /p:Platform=x64 /m
.\rag2.0.tests\x64\Debug\rag2.0.tests.exe
```
Expected：0 编译错误；全量绿（161，0 failed —— 本任务无新单测，`chunks_containing` 尚未被调用）。

- [ ] **Step 4: Commit**

```powershell
git add src/db/pg_client.h src/db/pg_client.cpp
git commit -m @'
feat(db): PgClient::chunks_containing (keyword LIKE on embedding_text)

按 embedding_text 子串直查全部含关键词片段（现行过滤），供列举题补全召回。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
'@
```

---

## Task 3: 接入 `text_retrieve` + 实测覆盖提升

**Files:**
- Modify: `src/retrieve/text_search.cpp`

- [ ] **Step 1: 改 `src/retrieve/text_search.cpp`**

先 READ 当前文件。顶部 include 区加：
```cpp
#include <set>
```

把这一行：
```cpp
    std::vector<Candidate> fused = rrf_fuse(lists, /*k=*/60, top_k);
```
整体替换为：
```cpp
    // M4.1：列举 + 命中关键词 → 关键词直查补全召回 + 错片下压
    std::vector<std::string> key_hit_ids;
    const bool list_recall =
        (qa.intent == QueryIntent::ListByCondition && !qa.key_terms.empty());
    if (list_recall) {
        std::vector<Candidate> kt;
        std::set<std::string> seen;
        for (const auto& term : qa.key_terms) {
            for (const auto& row : pg.chunks_containing(term, filter.status)) {
                if (seen.insert(row.chunk_id).second) {
                    Candidate c;
                    c.standard_id = row.standard_id;
                    c.chunk_id = row.chunk_id;
                    c.score = 0.0f;
                    c.source = "keyterm";
                    kt.push_back(c);
                    key_hit_ids.push_back(row.chunk_id);
                }
            }
        }
        lists.push_back(std::move(kt));
        spdlog::info("[queryplan] keyterm 直查命中 {} 片段", key_hit_ids.size());
    }

    // 列举时融合到大池、稍后下压再截断；非列举维持原 top_k 截断行为。
    const int fuse_k = list_recall ? 1000000 : top_k;
    std::vector<Candidate> fused = rrf_fuse(lists, /*k=*/60, fuse_k);
    if (list_recall)
        fused = demote_without_keyterms(fused, key_hit_ids, top_k);
```

其余（dense/bm25/方法号路、`pin_exact_clause` 块、返回）保持不动。`chunks_containing` 经 `db/pg_client.h`（已被 `text_search.h` 包含）可见；`demote_without_keyterms` 经 `retrieve/rrf.h`（已包含）可见。

- [ ] **Step 2: 构建 + 全量测试**

```powershell
& "D:\vs2022\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" rag2.0.sln /p:Configuration=Debug /p:Platform=x64 /m
.\rag2.0.tests\x64\Debug\rag2.0.tests.exe
```
Expected：0 错误；全量绿（161，0 failed）。

- [ ] **Step 3: 确认 Milvus 在跑**

```powershell
docker ps --filter "name=milvus-standalone" --format "{{.Names}} {{.Status}}"
```
Expected：`milvus-standalone Up ... (healthy)`。未起则按 [[local-services]] 启动（先 `docker start milvus-etcd milvus-minio` 再 `docker start milvus-standalone`），等 healthy。

- [ ] **Step 4: 实测覆盖提升（核心验收）**

```powershell
$OutputEncoding=[Text.Encoding]::UTF8; [Console]::OutputEncoding=[Text.Encoding]::UTF8
.\rag2.0\x64\Debug\rag2.0.exe eval eval/dataset_seed.json 30 2>&1 | Out-String
```
Expected：
- 日志出现 `[queryplan] keyterm 直查命中 N 片段`（N 应 ≥ 24，天平片段全召回）；
- 天平用例 `coverage@30` 从 baseline 的 ~12/23 **大幅上升，目标接近 23/23**（验收门槛 ≥ 18/23）；
- 两条方法点查仍 `hit@1`（`点查 hit@30: 2/2`）。

记录实际 coverage 数字。若未达 ≥18/23，**先排查 `chunks_containing` 是否真召回了全部天平片段**（可临时把 `[queryplan] keyterm 直查命中 N` 的 N 与库里"embedding_text 含天平"的片段数对比），不要乱调；按 systematic-debugging 定位。

- [ ] **Step 5: 回归实测（普通/编号题不变差）**

```powershell
$OutputEncoding=[Text.Encoding]::UTF8; [Console]::OutputEncoding=[Text.Encoding]::UTF8
.\rag2.0\x64\Debug\rag2.0.exe eval eval/dataset_seed.json 20 2>&1 | Out-String
foreach ($q in @("路基沉降怎么评定","T0302 需要哪些仪具")) {
  Write-Output "==== $q ===="
  .\rag2.0\x64\Debug\rag2.0.exe retrievecheck "$q" 8 2>&1 | Select-String -Pattern "queryplan|召回|^\d+\s" | Out-String
}
```
Expected：
- `eval … 20` 点查仍 2/2（方法号精确路不受影响）；
- `路基沉降怎么评定`：`intent=GeneralFact`，**无** `keyterm 直查命中` 日志（list_recall=false），结果与改前一致；
- `T0302 需要哪些仪具`：`intent=MethodLookup`，无 keyterm 直查，方法号路照旧。

- [ ] **Step 6: Commit**

```powershell
git add src/retrieve/text_search.cpp
git commit -m @'
feat(retrieve): list-query recall completion + demotion in text_retrieve

列举+命中关键词时：PG 直查补全召回并入融合大池，按关键词命中下压重排再截断；
天平列举题 coverage 大幅提升，普通/编号题零回归。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
'@
```

---

## Self-Review

**1. Spec 覆盖：**
- §2.1 召回补全 PG 直查路 → Task 2（`chunks_containing`，`embedding_text LIKE` + 现行 join）+ Task 3 Step 1（并入融合）。
- §2.2 大池上下压再截断 → Task 1（`demote_without_keyterms`）+ Task 3 Step 1（`fuse_k=大池` → demote → top_k）。
- §2.3 仅 `ListByCondition`+`key_terms` 生效 → Task 3 `list_recall` 闸门；其余意图 `fuse_k=top_k`、不下压。
- §2.4 字段 `embedding_text`、`status='现行'` → Task 2 SQL。
- §2.5 不自动放大 k；验证用更大 k → Task 3 Step 4 用 `eval … 30`。
- §6 验收（coverage 提升、点查不回归、纯函数单测、不改 schema）→ Task 1 Step 4 + Task 3 Step 4/5。

**2. Placeholder 扫描：** 无 TBD/TODO；每步给完整代码 + 确切命令 + 预期。`fuse_k=1000000` 是"足够大以不提前截断"的明确值（融合并集远小于它），非占位。

**3. 类型一致性：** `demote_without_keyterms(const std::vector<Candidate>&, const std::vector<std::string>&, int)` 在 rrf.h/.cpp/test/text_search 一致；`chunks_containing(keyword,status)→std::vector<RetrievalChunkRow>`（`chunk_id`/`standard_id`）在 pg_client.h/.cpp 与 text_search 调用处一致；复用 `QueryAnalysis.intent/key_terms`、`RetrievalFilter.status`、`Candidate{standard_id,chunk_id,score,source}`、`rrf_fuse(lists,k,top_k)`——均与现有签名一致。

**4. 已知限制：** 若某 gold 片段 `embedding_text` 里确无"天平"字样则仍召回不到（spec §7 已记，需核查口径）；coverage@k 上限 = min(k, gold 数)，故验证用 k=30。
